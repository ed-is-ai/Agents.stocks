---
name: ordinary-skill
description: >
  An ordinary Skill with no Strategy kind declared. Discovery must silently
  ignore it -- no descriptor, no warning.
---

# Ordinary Skill (discovery fixture)

Mirrors the plain `name:`/`description:` frontmatter every real Skill in
`skills/` uses today.
