## Deferred from: code review of stories 1.8 and 1.9 (2026-08-12)

- Zero and negative Running Balance values are still discarded during import at `app/agents/trader/trader_agent.py:901`; this is pre-existing and belongs to Story 1.5's storage-side cash-balance handling.
- Running Balance `N/A` normalization remains case-sensitive at `app/agents/trader/trader_agent.py:902`; this is outside Stories 1.8/1.9.
- Cash-only negative balances retain legacy positive-position styling at `app/api/templates/_portfolio.html:453`; this is outside the stated Stories 1.8/1.9 acceptance criteria.

## Deferred from: code review of 1-9-stale-free-presentation (2026-08-12)

- Zero and negative Running Balance are still discarded at ingest (`app/agents/trader/trader_agent.py:903`), so a genuinely drained account can never persist a zero. Pre-existing, Story 1.5's scope. A review layer rated this High on the theory that Story 1.9 "launders" a stale balance into the response; verified false — before the change the result reported `0.0` while storage held the old balance, so 1.9 made response, page and snapshot agree, which is its AC1.
- The two error paths in `refresh_portfolio_prices` (`app/api/routes/portfolio.py:104,140`) build the partial context without `cash_balance`, so it defaults to `None` and a cash-only portfolio loses its CASH row on any 400/500 render. Pre-existing; the truthiness gate hid it equally before.
- `_write_import_snapshot`'s `round(...) if cash_balance is not None else None` branch (`app/agents/trader/trader_agent.py:1173`) is now unreachable from the import path, so `portfolio_snapshots.cash_balance` records a hard `0` rather than NULL for a balance-less portfolio — the value-history chart shows zero instead of a gap. Entangled with the phantom-`£0.00` decision item.
- `tests/test_portfolio_template.py` duck-types `Position` with a 16-field `SimpleNamespace` and renders via `get_template(...).render()` rather than `TemplateResponse`, so `request`/`url_for` are absent. Schema field drift or any future `url_for` in the template would pass these tests and fail in production.
- Story 1.9's spec is stale: `baseline_commit` 72b8d72 is not an ancestor of HEAD, Gate 0's "none of Stories 1.1–1.8 have landed" no longer holds, and Gate 1's prescribed `update_portfolio_snapshot` fix would have been a no-op for the multi-portfolio snapshot leg (the snapshot is now written inside the transaction by `_write_import_snapshot`). The implementation adapted correctly; the story doc was never updated to say so.
- A portfolio with no cash balance on record renders a phantom `CASH £0.00` row after import: `get_cash_balance()` returns `None` but `effective_cash_balance` coerces to `0.0`, and `SippImportResult.cash_balance` is a non-optional `float`, so `app/api/routes/portfolio.py:260` feeds `0.0` to the template and the `{% if cash_balance is not none %}` gate fires. Deferred: blocked on Story 1.5 — the zero/negative ingest gate makes the None-vs-zero distinction unobservable through the import path anyway. When 1.5 lands, revisit: either make `SippImportResult.cash_balance` optional (contradicts 1.9's spec constraint and touches 1.8's rejected-path `0.0`) or have the route re-read storage.
  **Resolved 2026-08-13:** Story 1.5 has since landed. Took the re-read option: `import_sipp`'s success path in `app/api/routes/portfolio.py` now builds the template context from `trader.get_cash_balance(pid)` (fresh from storage, `None` when unset) instead of `result.cash_balance`. `SippImportResult.cash_balance` is unchanged. Regression: `tests/test_portfolio_import.py::test_import_with_no_cash_balance_row_never_shows_a_phantom_zero`.

## Deferred from: code review of story-1-10-bounded-post-commit-archive (2026-08-13)

- Unsanitized `filename` (not `safe_name`) is logged in the new `logger.warning` call in `_archive_upload`'s size-cap-skip branch (`app/api/routes/portfolio.py`) — log-injection via a crafted filename with embedded control characters. Pre-existing: the neighboring untouched `logger.exception("Failed to archive imported file %s", filename)` already has this exact pattern; this story's new line only continues it.
- `_prune_old_archives()` (`app/api/routes/portfolio.py`) adds a synchronous directory-scan/stat/unlink loop to the async `/import-sipp` request path with no executor offload, blocking the event loop for its duration. Pre-existing pattern: `_archive_upload`'s `mkdir`/`write_bytes` were already synchronous file I/O directly in this same async handler before this story. `IMPORTED_FILES_DIR` realistically holds a handful of files for a single-user local app under a 7-day retention window, so the added scan cost is not meaningful at this app's actual scale — revisit if usage patterns change.

## Deferred from: code review of story-2-4-explainable-unmatched-sells (2026-08-14)

- A same-day Opening Lot cannot retroactively resolve a same-day unmatched sell: `_replay_sort_key` (`app/services/realised_pnl_service.py`) and `_REPLAY_ORDER` (`app/repositories/trades_repo.py`) tie-break same-date rows with no `source_row_index`/`idempotency_key` by ascending `id`, so a manually-entered Opening Lot (always written after the sell it's meant to fix) always replays *after* that sell on a tie, even when dated the same day. AC9's own I/O-matrix wording only promises correctness for an Opening Lot dated *before* the sell, so this is outside the literal contract, but it's the single most natural user workflow (backdating to "today" while looking at the unmatched-sell banner). The underlying ordering gap is not fixed: `_replay_sort_key`/`_REPLAY_ORDER` are shared replay-ordering infrastructure spanning Stories 2.2/2.3 and every other feature that depends on deterministic replay order — a same-day special case for Opening Lots specifically would need careful design to avoid destabilizing that shared logic, which is out of proportion to this story's scope.
  **Mitigated 2026-08-14:** added `_same_day_ordering_warning` (`app/api/routes/portfolio.py`), called by both `create_opening_lot` and `edit_opening_lot` after the write. It detects the exact failure signature (the lot is still fully unconsumed *and* an unmatched sell remains for the same ticker/date) and surfaces a warning banner naming the gap and the one-day-earlier workaround, instead of a plain success message with no indication anything went wrong. Regression tests: `test_create_opening_lot_warns_on_same_day_ordering_gap`, `test_create_opening_lot_no_warning_when_lot_was_consumed`, `test_edit_opening_lot_warns_on_same_day_ordering_gap` in `tests/test_opening_lot_routes.py`.
- `correct_trade` (`app/agents/trader/trader_agent.py`) unconditionally calls `delete_by_ticker`, which silently deletes any `source='opening_lot'` row for that ticker/portfolio — consumed or not. This bypasses the AC7/AC8 "read-only once consumed" guarantee this story establishes, but only via `correct_trade`'s route, not the Opening Lot edit/delete routes it explicitly gates. `correct_trade`'s pre-existing contract ("overwrite this ticker's position: delete all trades, insert one BUY") already deletes SELLs unconditionally, so this is consistent with an existing, already-shipped "blunt reset" tool rather than a new class of behavior — but the interaction with Opening Lots specifically was never addressed in this story's Boundaries & Constraints. Deliberately not blocked: changing `correct_trade`'s no-precondition contract, or special-casing it to preserve Opening Lots, are each real design decisions with their own tradeoffs (see the spec's Review Triage Log) beyond a same-review fix.
  **Mitigated 2026-08-14:** `POST /trades` (`app/api/routes/trades.py`, the `CORRECT` branch) now checks `get_trade_history(ticker, pid)` for a `source == "opening_lot"` row before calling `correct_trade`, and if found, still proceeds (contract unchanged) but adds a warning banner naming what was deleted, instead of the user finding out only if they happen to notice the position looks different later. Regression tests: `test_correct_warns_when_it_deletes_an_opening_lot`, `test_correct_no_warning_when_no_opening_lot_present` in `tests/test_trades_routes.py`.
- No DB-level uniqueness backstop for Opening Lot duplicate detection: `_opening_lot_duplicate_check` (`app/agents/trader/trader_agent.py`) is check-then-insert with no supporting unique index (unlike SIPP import's `idx_trades_portfolio_idempotency`), and the Opening Lot edit/delete routes (`app/api/routes/portfolio.py`) read `opening_lot_status` then mutate with no transaction spanning the gap — both are genuine TOCTOU windows. Low practical risk given this is a single-user local app behind `require_local_or_token`, not fixed here to avoid a same-review schema/transaction change beyond this story's scope.
- `partial_history` (`app/api/routes/views.py`) calls `RealisedPnlService.opening_lot_status` once per Opening Lot trade shown, and each call runs its own full `_replay_fifo` — so a portfolio with several Opening Lots re-replays that portfolio's whole trade history once per lot on a single Trade History view load. Consistent with this story's explicit "recompute fresh, never cache" design, so not a correctness bug; not fixed here since Opening Lot entry is a manual, occasional action (realistically a handful per portfolio in this single-user app), making the redundant work immaterial at actual scale. Revisit (e.g. one `_replay_fifo` per portfolio, checked against every lot id) if usage patterns change.

## Deferred from: code review of spec-2-1-author-a-strategy-against-the-versioned-protocol (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-author-a-strategy-against-the-versioned-protocol.md`
  summary: Every real Strategy will use the literal filename `skills/<strategy-name>/scripts/strategy.py` per `docs/strategy-manager/strategy-authoring-v1.md`'s convention, and each Strategy's `scripts/tests/conftest.py` does a bare `sys.path.insert` + `from strategy import ...` (mirroring the existing per-Skill conftest pattern) — once a second real Strategy exists, its tests and the first Strategy's tests collected in the same `uv run pytest` session will collide on `sys.modules["strategy"]`, silently testing whichever Strategy's module was imported first.
  evidence: Confirmed via `find skills -name conftest.py`: every existing Skill (vcp-screener, finviz-screener, canslim-screener, market-top-detector, institutional-flow-tracker, breakout-trade-planner) already uses this exact `sys.path.insert` pattern safely today only because each Skill's main script has a distinct name (e.g. `vcp_screener.py`); the new Strategy convention is the first to standardize on one filename (`strategy.py`) across every instance, which is precisely what makes the collision reachable. Zero blast radius today (`tests/fixtures/backtest-strategies/minimal-strategy/` is still the only Strategy in the repo) — revisit when Story 2.2 or a real Strategy Skill adds a second `scripts/strategy.py` under test.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-author-a-strategy-against-the-versioned-protocol.md`
  summary: The parametrized `test_strategy_source_manifest_rejects_non_runtime_allowlists` (and its pre-existing sibling for detector manifests) only asserts `pytest.raises(ValueError)` for four structurally different invalid-allowlist cases (wrong directory, duplicate entry, path traversal, missing file) without checking which validation reason fired, so a regression that broke one specific check but tripped a different, unrelated `ValueError` would pass undetected.
  evidence: Confirmed the generic `pytest.raises(ValueError)` (no `match=`) pattern already exists at `tests/backtest/test_source_manifest.py:77` for the pre-existing detector-manifest allowlist tests, predating this story — the new Strategy manifest tests only followed the established repository convention rather than introducing a new gap. Worth tightening across the whole file in one pass rather than piecemeal.

## Deferred from: implementation of spec-2-2-discover-strategies-and-validate-their-parameters (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-discover-strategies-and-validate-their-parameters.md`
  summary: `uv run ruff format .` (repo-wide, no path filter) reformats roughly 33 files outside this story's scope, indicating pre-existing formatting drift across the repository unrelated to Strategy Manager work.
  evidence: Confirmed by running the implementation subagent's verification commands as specified (`ruff format .` with no path arg) and observing the diff include files never touched by this story; the story's own 4 touched files are independently clean under both `ruff format --check` and `ruff check` when checked directly. Worth a dedicated repo-wide formatting chore, done deliberately rather than as a side effect of some future story's verification pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-discover-strategies-and-validate-their-parameters.md`
  summary: `tests/test_portfolio_import_queue_browser.py`'s Playwright browser tests are intermittently flaky under full-suite load (a different test fails each run, always passes standalone or in a smaller subset).
  evidence: Reproduced directly: a full `uv run pytest -q` run failed `test_discard_reports_files_already_imported_mid_batch_switch` on a file-input locator timeout; re-running that single test alone failed differently (a detached-DOM element during a dropdown click) then passed on a second standalone attempt. Unrelated to this story, which touches no browser/portfolio-import code. Pre-existing test-infra flakiness, likely load/timing-sensitive Playwright waits; worth a dedicated look at this file's wait conditions.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-discover-strategies-and-validate-their-parameters.md`
  summary: The `sys.modules["strategy"]` collision risk flagged in Story 2.1's review (`deferred-work.md` above) remains open and unresolved as of Story 2.2, though not triggered by it.
  evidence: This story's new discovery fixtures under `tests/fixtures/backtest-strategies/discovery/` (`valid-strategy`, `duplicate-strategy-one`, `duplicate-strategy-two`) each add a `scripts/strategy.py`, per the spec's own Design Notes guidance to avoid re-triggering the collision, none of them ship a `scripts/tests/conftest.py` that imports `strategy` unqualified, since discovery never executes that module. The collision is therefore still zero blast radius today, but three same-named `strategy.py` files now exist in the repo under test fixtures alone (plus `minimal-strategy`'s) — revisit when a real Strategy Skill under `skills/` first ships its own `scripts/tests/` contract-test suite.

## Deferred from: code review of spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `RunInputManifestV1.market_view_source_digest`'s allowlist (`_MARKET_VIEW_ALLOWLIST` in `run_input_manifest.py`) excludes `app/repositories/backtest_repo.py` entirely, even though `MarketView.scan_result` delegates its whole visibility-timing rule (`latest_committed_scan_result`) to that file -- a genuine future change to that query's semantics would not bump `market_view_source_digest`/`execution_contract_digest`, so two Runs with materially different scan-visibility behavior could still register as AD-19-comparable.
  evidence: Confirmed by reading `_MARKET_VIEW_ALLOWLIST` (five files, `backtest_repo.py` absent) against `MarketView.scan_result`'s actual implementation, which calls `self.backtest_repo.latest_committed_scan_result(...)` as its sole visibility authority. The narrow allowlist is a deliberate, documented tradeoff (avoiding unrelated churn elsewhere in that large file spuriously bumping the digest) rather than an oversight, but the tradeoff's cost is real and undocumented. Revisit once Story 2.4/2.6 exercises `execution_contract_digest` comparisons in anger -- likely fix is splitting the scan-visibility query into its own small module included in the allowlist.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `MarketView.scan_result` applies no restriction to the per-security evidence universe pinned in `RunInputManifestV1.securities` (unlike `.price_history`, which returns an empty frame for anything untracked) -- a Strategy can discover a scan candidate via `scan_result` whose price evidence was never pinned, and `.price_history` for it then silently returns an empty frame rather than a `MarketViewBoundError`/`evidence_missing` failure.
  evidence: Confirmed `scan_result` depends only on `(profile_hash, security_id, as_of_session)`, independent of `security_price_revisions`, matching this story's own spec I/O matrix ("Unknown security, in-bound date -- .scan_result returns None, not a bound violation"). The deeper question -- whether `RunInputManifestV1.securities` must pre-pin every roster member a Strategy could plausibly discover via scan, not just ones a caller already knows about -- is Story 2.4's responsibility (it defines execution order and which securities get evidence-pinned before a Run starts), not resolvable within this story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `build_run_input_manifest` never validates that a pinned security's evidence interval overlaps `[start_month, end_month]` at manifest-construction time -- that failure is deferred entirely to `MarketView.price_history` raising `MarketViewBoundError` later, inside Story 2.4's not-yet-built engine, rather than failing fast and cheaply where the problem is easy to diagnose.
  evidence: Confirmed no such check exists in `build_run_input_manifest` or `_verify_pinned_evidence` (`run_input_manifest.py`) -- both only verify a pinned revision *resolves* and belongs to the right security, never that its `[start, end)` covers the requested range. Not a correctness bug (the bound is still enforced, just later and by a different component), and no AC in this story's `<intent-contract>` requires eager range validation -- a fail-fast UX improvement worth revisiting once Story 2.4/2.7 build the caller that assembles `securities` and can surface this error to a human before a Run is queued.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: No check that a pinned `security_id` in `RunInputManifestV1.securities` is actually a member of the roster tied to `resolved_profile_hash` -- a caller can pin evidence for securities with no relationship to the resolved profile and the manifest still builds.
  evidence: Confirmed `build_run_input_manifest` never cross-checks `securities` against `profile.roster_digest`'s membership. No explicit AC in this story requires it, and legitimate designs exist where a Strategy trades securities outside the scan roster (e.g. a fixed watchlist parameter) -- revisit once Story 2.4/2.6 defines how `securities` actually gets assembled and whether roster-membership is meant to be a hard constraint.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `RunInputManifestV1._exact_detector_set` validates `detector_source_digests` against the *current* `DETECTOR_REGISTRY`, not a registry snapshot pinned to the manifest's own creation time -- reconstructing (`model_validate`) any previously-persisted manifest after a new detector is later registered would fail validation, even for pure read-only historical replay.
  evidence: Confirmed the field validator (`run_input_manifest.py`) computes `expected = {d.detector_id for d in DETECTOR_REGISTRY}` fresh on every validation call, with no versioning/snapshotting. Not reachable today -- this story persists nothing and never calls `model_validate` on stored data (no persistence exists until Story 2.5/2.6). Revisit when Story 2.6 builds manifest persistence/reload: either accept this as an intentional "old Runs become unreplayable after a detector-registry change" tradeoff, or snapshot the expected detector set at construction time instead of re-deriving it at validation time.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `_ledger_action_metrics_digest`'s deterministic placeholder (`LEDGER_ACTION_METRICS_POLICY_VERSION = "ledger_action_metrics.v1"`) relies entirely on a future implementer remembering to bump the version string once Story 2.4 builds real `ledger.py`/`action.py`/`metrics.py` files -- nothing enforces the bump, so a forgotten update would silently make pre- and post-2.4 `execution_contract_digest`s compare equal when they should differ.
  evidence: Confirmed via `run_input_manifest.py`'s module-level comment on `LEDGER_ACTION_METRICS_POLICY_VERSION`, which already documents this exact risk and instructs the future fix (replace the placeholder function with a real `build_source_manifest` call over the landed files). No test asserts the bump happens. Revisit as part of Story 2.4's own definition of done -- add a regression test then that fails if the placeholder function is still in use once the real files exist.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `build_run_input_manifest`'s `start_month`/`end_month` parameters are plain, unvalidated `str` until the very end (`RunInputManifestV1`'s own `SnapshotMonth`-pattern field validator is the first thing that would reject a malformed month) -- a malformed month string reaches `backtest_repo.interval_readiness`/`TradingCalendar.months_inclusive` first, which may raise a less-structured, non-`RunInputManifestError` exception instead of this builder's own stable-code convention.
  evidence: Confirmed every other precondition failure in `build_run_input_manifest` (`no_active_profile`, `unknown_profile`, `coverage_incomplete`, `unknown_roster`, `invalid_parameters`, plus this pass's new `invalid_securities`/`evidence_mismatch`) raises `RunInputManifestError` with a stable code, but a malformed month string bypasses all of them and reaches `interval_readiness` unvalidated. Low severity -- the call still fails safely, just with an inconsistent exception type/no code. Worth a small early-validation patch in a future pass; deferred here to keep this review's patch set focused on the higher-value fixes (FX evidence resolving via the wrong repository, an unenforced currency invariant, `base_currency` accepting non-GBP/USD codes, `securities` empty/duplicate falling through to a raw `pydantic.ValidationError`).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: `RunInputManifestV1.parameters` is typed `dict[str, object]` rather than the codebase's existing `JsonValue`-recursive alias, so the model's own type contract doesn't enforce JSON-safety on direct construction (only the `build_run_input_manifest` builder path is actually safe, via `validate_strategy_parameters`).
  evidence: Attempted `dict[str, JsonValue]` (matching `strategy_protocol.StrategyParameters = Mapping[str, JsonValue]`) and hit `RecursionError: maximum recursion depth exceeded` building the pydantic core schema -- `JsonValue`'s self-referential `JsonScalar | list["JsonValue"] | Mapping[str, "JsonValue"]` alias appears never to have been exercised as an actual pydantic model field anywhere in the codebase (only as Protocol method type hints and plain-function signatures), and reproducing it as a real field type here triggers a pydantic v2 schema-generation bug/limitation worth its own focused investigation before reattempting. Reverted to the original, safe `dict[str, object]` rather than risk destabilizing a verified, passing module.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-bind-backtests-to-bounded-views-and-a-replay-manifest.md`
  summary: Substantial test-fixture duplication -- `_evidence_for_record`, `_Verifier`/equivalent, `_roster_commit`, `_backtest_repo`, `_price_repo`, `_authoritative_detectors`, `_profile` are near-identical, copy-pasted across `tests/backtest/test_market_view.py`, `tests/backtest/test_run_input_manifest.py`, and (per an in-code comment) `tests/backtest/test_snapshot_coverage_repository.py`.
  evidence: Confirmed by reading all three files' fixture sections side by side. Any future schema change to these fixtures (e.g. a new required field on `SnapshotProfileV1`) now has to be replicated by hand in three places. Worth extracting to a shared `tests/backtest/conftest.py`/helper module in a dedicated chore, not attempted here to avoid touching test files this story doesn't own.

## Deferred from: code review of story-2-4-execute-the-deterministic-portfolio-simulation (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-execute-the-deterministic-portfolio-simulation.md`
  summary: `_Engine.__init__` cross-checks each supplied security's `price_evidence.data_revision` against its pinned `price_revision` (and, after this pass's patch, `fx_evidence` against `fx_revision`), but never cross-checks a pinned `action_revision` against anything -- the engine implicitly assumes `action_revision == price_revision` and derives corporate actions straight from `price_evidence`'s own plane, so a Run pinned with a genuinely divergent `action_revision` (a case `run_input_manifest.py`'s own `_verify_pinned_evidence` already supports, fetching a separate `action_evidence` from the repository when the two revisions differ) would silently apply the wrong -- or no -- action evidence.
  evidence: Confirmed `SecurityMarketDataV1` (`backtest_engine.py`) carries only one `price_evidence: StoredHistoricalEvidence` field with no matching `action_evidence` field, and `_apply_actions`/`__init__` read corporate actions exclusively via `plane.actions_as_of(...)` off that same object. `run_input_manifest.py`'s own docstring on `PinnedSecurityEvidenceV1` calls `action_revision`/`price_revision` "usually the same value" but keeps them as two named fields precisely so a future evidence model that does separate them "needs no shape change here" -- for the manifest. The engine has no equivalent accommodation. Not reachable today (every caller in this story's own tests and the manifest builder always pins the two revisions equal), and properly supporting divergence needs a real shape change (`SecurityMarketDataV1` gaining an `action_evidence` field, threaded through `run_simulation`'s public API and every caller) rather than a one-line check, so it wasn't attempted as a same-pass patch. Revisit alongside Story 2.5/2.6 if a real evidence-mismatch scenario becomes reachable.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-execute-the-deterministic-portfolio-simulation.md`
  summary: A `StrategyProtocolV1` implementation (or an injected `market_view_factory`) that raises a non-`StrategyProtocolError` exception (e.g. a plain `TypeError`/`KeyError` from buggy Strategy code) propagates out of `run_simulation` as that raw exception type instead of the documented single `SimulationError` contract.
  evidence: Confirmed no `except Exception` boundary wraps the `entry_signals`/`exit_signals`/`position_size`/`market_view_factory` call sites in `_Engine`; only `validate_entry_signals`/`validate_exit_signals`/`validate_position_size`'s own `StrategyProtocolError` is caught downstream. This story's AC 6 enumerates a specific closed set of fatal categories (unsupported action, FX, missing evidence, arithmetic/invariant failure) that does not include "arbitrary Strategy bug" -- Strategy-authoring correctness is Stories 2.1/2.2's validation boundary, not this engine's. Worth deciding explicitly once Story 2.6 (the worker that actually invokes `run_simulation` unattended) needs one exception type to catch for its own fallback/dead-lettering, not blocking this story's own acceptance criteria.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-execute-the-deterministic-portfolio-simulation.md`
  summary: A pinned corporate action dated on a session that isn't a valid trading session in its own security's `TradingCalendar`/MIC (malformed/edge-case evidence) is silently never applied -- no event, no error -- because `_apply_actions` only ever looks up `self.actions_by_security[security_id].get(session, ())` for sessions the engine's own union-calendar loop actually visits, and a security-specific non-trading-day action session is never one of those.
  evidence: Confirmed `_apply_actions` has no fallback/validation path for an action bucketed under a date absent from `self.session_index[security_id]`. Real corporate actions (splits/dividends) are effective-dated on genuine trading sessions by definition, and this evidence is produced by Epic 1's ingestion pipeline upstream of this story, so the practical trigger requires already-malformed evidence -- the same category of "trust the upstream pipeline" tradeoff Story 2.3's own triage log already accepted for adjacent evidence-shape assumptions. Worth a defensive fatal check in a later pass, not blocking today.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-execute-the-deterministic-portfolio-simulation.md`
  summary: The engine invokes a Strategy's `entry_signals`/`exit_signals` once per *union-calendar* session (any day that is a trading session for *any* pinned security), not gated per security, so a Strategy can emit a `Signal` dated on a day its own target security's own market happens to be closed; `_schedule_signal` only checks the signal's self-consistency (`signal.session != session`) and never checks that `session` is actually a trading day for that specific `security_id`.
  evidence: Confirmed `_schedule_signal` has no such check, and no test in `test_backtest_engine.py` exercises this combination. Traced the consequence: `_next_mic_session` already resolves strictly from that security's own MIC calendar independent of the signal's session, so the fill still lands on a genuine future trading session for that security -- this does not appear to break any invariant, just accepts an oddly-dated signal without complaint. Low-value/low-risk; worth a regression test asserting the currently-benign behavior explicitly in a later pass rather than treating it as a live bug.

## Deferred from: code review of spec-2-5-calculate-and-atomically-persist-backtest-results (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-calculate-and-atomically-persist-backtest-results.md`
  summary: Any `MetricsError`/`BacktestIntegrityError` raised inside `complete_claimed_backtest_job` (e.g. a genuinely reachable zero-equity-mid-curve business state, where `metrics.py`'s `_daily_returns` divides by a zero-equity point) propagates as an uncaught exception with no distinct job-failure signal beyond that -- the job is left `running` forever unless the caller catches it and transitions the job to `failed` itself.
  evidence: Confirmed `complete_claimed_backtest_job` has no `except` around its `calculate_metrics(...)` call, and this story's own AC6 explicitly delegates the resolution: "Story 2.6 fallback can diagnose the stable failure and mark the job failed." `MetricsError` now re-raises as `BacktestIntegrityError` (this pass's own patch) so the exception surface is at least stable/typed for that fallback to catch -- but Story 2.6 must actually build a catch-and-fail wrapper around this call; nothing here does it automatically. Revisit when Story 2.6 implements the worker/fallback path.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-calculate-and-atomically-persist-backtest-results.md`
  summary: `update_backtest_result_note` always HTML-escapes its input (`html.escape`) and `backtest_result()` returns the already-escaped stored value, so a caller that naively resubmits the previously-returned `note` as new raw input on a subsequent edit will double-escape entities each round-trip.
  evidence: Confirmed `_normalize_note` unconditionally calls `html.escape(stripped, quote=True)` with no idempotency guard, and `backtest_result()` returns `note` straight from storage with no unescape step. This story builds only the repository method, not the UI (Story 2.9's scope) -- flagging so Story 2.9's edit form always sources its textarea from the user's own typed input, never by pre-filling from `backtest_result().note` and resubmitting that value unmodified.

## Deferred from: code review of spec-2-6-run-backtests-through-the-durable-activity-lifecycle (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-run-backtests-through-the-durable-activity-lifecycle.md`
  summary: `PinnedSecurityEvidenceV1.action_revision` is still never resolved, validated, or cross-checked anywhere in the worker's real evidence-resolution path -- only `price_revision` is used, exactly the gap Story 2.4's own review already flagged.
  evidence: Confirmed `worker.py`'s `_resolve_security` reads only `item.price_revision` and `market_view_factory` builds `security_price_revisions` from `price_revision` alone; `action_revision` is read nowhere in this story's new code either. This is the second consecutive story to surface the same pre-existing gap without resolving it -- Story 2.4's own deferred-work.md entry explicitly named "Story 2.5/2.6" as the point to revisit it "if a real evidence-mismatch scenario becomes reachable," and Story 2.6 is exactly that: the first story with a real, unattended worker caller. Still not fixed here because it needs a `backtest_engine.py`/`SecurityMarketDataV1` shape change (a dedicated `action_evidence` field threaded through `run_simulation`'s public API and every caller), which Story 2.4's own note already identified as "not a one-line check" and explicitly out of this story's `<intent-contract>` (no recalculation of Story 2.1-2.5's engine logic). Revisit as its own focused fix now that two stories have surfaced it independently.

## Deferred from: code review of spec-2-7-2-8-configure-launch-and-monitor-strategy-web-ui (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-7-2-8-configure-launch-and-monitor-strategy-web-ui.md`
  summary: `BacktestLaunchService`'s `_CURRENCY_PAIR`/`_fx_pair` collapses both FX directions (GBP-base/USD-security and USD-base/GBP-security) onto the same `"GBPUSD=X"` pair symbol and pins the same quote digest either way, with no recorded indicator of which currency is being converted into which.
  evidence: Confirmed `_fx_pair` returns the identical `"GBPUSD=X"` string regardless of argument order, and `PinnedSecurityEvidenceV1.fx_revision` stores only a bare digest. A regression test for the reverse (USD-base) direction was added this pass (`test_launch_usd_base_currency_pins_fx_for_a_gbp_security`) and passes, but it only proves a quote gets pinned, not that the pinned quote's rate is later applied in the economically correct direction during simulation/valuation -- that depends on `FxQuoteRepository`/the currency-conversion consumer's own semantics (likely Story 1.2's `CurrencyConversion`), which this story never needed to touch and which was not investigated here. Revisit by tracing one full USD-base, GBP-security Run's FX conversion end to end (or reading `CurrencyConversion`'s digest-interpretation contract directly) to confirm direction-safety, rather than assuming it from this story's narrow surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-7-2-8-configure-launch-and-monitor-strategy-web-ui.md`
  summary: A single Backtest launch POST triggers up to three independent Skill-discovery filesystem scans (`submit_strategy_configuration` calls `launch.discover()` directly, `_configuration_context()` calls it again via `launch.configuration()`, and `launch.launch()` calls it a third time via `_resolve_strategy()`), with no caching and no guarantee the three snapshots agree if a Skill changes mid-request.
  evidence: Confirmed via direct read of `submit_strategy_configuration`/`_configuration_context`/`BacktestLaunchService.launch`/`_resolve_strategy` -- each independently calls `discover_strategies(self._skills_root)`, which is explicitly documented as "never cached" by design (Story 2.2's freshness guarantee). Not fixed here because threading one resolved `StrategyDescriptorV1` through all three call sites touches the route/service boundary's calling convention (not a one-line change) and risks subtly changing the "always re-verify immediately before manifest construction" invariant this story's Boundaries explicitly require if done carelessly. Low real-world impact today (metadata-only scan over a small `skills/` directory), but worth a focused pass if the Skills directory or request volume grows.

## Deferred from: code review of spec-2-9-review-a-completed-backtest-result (2026-08-20)

- Top-level (non-htmx) navigation to any `/strategy-manager/*` route returns a bare, unstyled partial with no layout/CSS/JS wrapper, since none of these routes check the `HX-Request` header or wrap their template in `index.html`'s shell. Confirmed pre-existing across the whole router -- the existing `/strategy-manager/activities/{job_id}` route (Story 2.6/2.8) has the identical characteristic (`app/api/routes/strategy_manager.py:649`). Story 2.9's own `backtest_result_view` (`app/api/routes/strategy_manager.py:830-859`) inherits this rather than introducing it, though the consequence is worse here since a direct/bookmarked/middle-clicked visit silently drops the entire equity chart (Chart.js is only ever loaded in `index.html`'s `<head>`) with zero indication anything failed. A system-wide fix (HX-Request-aware full-page rendering) is out of scope for one story to take on unilaterally.
- `_result_context`'s `repo.snapshot_coverage(profile_hash=result.profile_hash)` call (`app/api/routes/strategy_manager.py:766-796`) is caught by the same blanket `BacktestIntegrityError` handler as `backtest_result()` itself, so a provenance/coverage-only failure blanks the entire Result page -- Metrics, Equity Curve, and Trade Log included -- rather than degrading gracefully. Verified unreachable under current invariants: no code path deletes rows from `snapshot_profiles` (`grep` for `DELETE FROM snapshot_profiles` across `backtest_repo.py` found none), so a profile pinned by an already-completed Backtest cannot vanish out from under it today. Revisit if a future profile-pruning/GC feature is ever added.
- A 409 note-save conflict's "Retry save" can overwrite a concurrently-saved note if the user retries without reading/merging the freshly-shown current text (`app/api/templates/_backtest_note.html:17-33`). CAS correctly rejects a *stale* write and the current persisted text is always shown before retry, so nothing is silently discarded per AC8's literal wording -- but nothing stops a user from blindly re-clicking Retry without looking. A stronger merge/diff UX would need explicit product input on how much friction is acceptable for what is likely single-owner double-tab editing rather than true multi-user contention.
- The equity-curve Chart.js instance in `_backtest_result.html:68-105` is destroyed-before-recreate on the same canvas (`Chart.getChart(canvas)?.destroy()` before `new Chart(...)`) but is never destroyed when the user navigates away from the Result page entirely via htmx (no `htmx:beforeSwap`/`afterSwap` cleanup hook). A minor Chart.js instance/listener leak across repeated navigation. Mirrors the codebase's one pre-existing Chart.js usage in `_portfolio.html`, which has no destroy-on-navigate-away handling at all -- Story 2.9 is already stricter than that precedent, just not fully leak-proof.

## Deferred from: code review of spec-3-1-enforce-one-canonical-comparison-eligibility-predicate (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-enforce-one-canonical-comparison-eligibility-predicate.md`
  summary: `is_comparable`/`comparison_candidates` string together multiple independent, uncoordinated `session()` calls with no cross-call transactional/snapshot consistency, so a job's status/tombstone state or Result could change between a candidate-listing query and its subsequent eligibility check.
  evidence: Confirmed `session()` (`app/repositories/db.py:23`) opens a fresh SQLite connection per call and `BacktestRepository` has no cross-method transaction/snapshot mechanism anywhere else in the file either -- this is a repo-wide pattern, not something introduced uniquely by Story 3.1. Not fixed here because no current mutation/delete path exists that could actually change a completed job's status or tombstone state mid-request (confirmed no code deletes/mutates `strategy_jobs`/`backtest_results` rows outside the durable job lifecycle itself), making the window theoretical today. Revisit if a future admin/delete-Result feature is added that could race with an in-flight comparison.

## Deferred from: code review of spec-3-2-choose-an-eligible-result-through-the-compare-picker (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-choose-an-eligible-result-through-the-compare-picker.md`
  summary: `_compare_context` (`app/api/routes/strategy_manager.py`) loads the anchor Result via `repo.backtest_result(run_id)` directly, then calls `repo.comparison_candidates(run_id)`, which internally re-loads the identical anchor Result a second time via its own `backtest_result(run_id)` call -- a redundant full Result fetch (Trade Log + Equity Curve included) on every picker render and every rejected 422 resubmit, and the second call is not wrapped in the same `StrategyJobNotFound`-to-`BacktestIntegrityError` guard the first one has.
  evidence: Confirmed via `app/repositories/backtest_repo.py:1917` (`comparison_candidates`'s own `anchor_result = self.backtest_result(run_id)` first line) -- this is Story 3.1's existing method shape, which Story 3.2's spec forbids modifying ("no schema/repository changes"). Not fixed here: within a single request both calls read the same unmutated row with nothing awaited in between (no interleaving is possible in this app's synchronous-within-request execution model), so the unguarded second call cannot currently diverge from the first, matching the "theoretical today" judgment already recorded for Story 3.1's analogous uncoordinated-`session()`-calls deferral above. Revisit together with that entry, and consider giving `comparison_candidates` an optional pre-loaded-anchor parameter if a future story needs to eliminate the redundant fetch for performance.

## Deferred from: code review of spec-3-3-review-two-results-side-by-side (2026-08-21)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-review-two-results-side-by-side.md`
  summary: `compare_picker`'s `reason` query param renders whatever text a caller supplies inside a `role="alert"` box with no binding to an actual `is_comparable` failure -- a directly-constructed `GET /strategy-manager/compare?run_id=X&reason=...` link can display fabricated "system" text, unlike `picker_error` on the POST 422 path, which is always server-computed from `eligibility.detail`.
  evidence: Confirmed `compare_picker` (`app/api/routes/strategy_manager.py`) accepts `reason: str | None` and folds it into `picker_error` verbatim with no origin/authenticity check beyond Jinja's default HTML autoescaping (confirmed active via `jinja2.select_autoescape()` in `app/api/templating.py`, which blocks script injection but not message spoofing). Not fixed here: a full fix (e.g. a validated `ComparisonIneligibleReason` code mapped server-side to canned copy, mirroring the picker's existing spec-mandated verbatim empty-state copy) is more than a trivial patch, and the practical severity is low given this is a single-user local tool (`CLAUDE.md`: "this project is a simple stock portfolio manager") with no other party to target and no other injection surface beyond a misleading alert. Revisit if the app's trust model ever changes (multi-user/hosted deployment).
- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-review-two-results-side-by-side.md`
  summary: `comparison_view` revalidates eligibility once via `is_comparable`, then `_comparison_context` performs two further independent, uncoordinated reads (`backtest_result` per side) with no re-check in between -- a pair that flips from eligible to ineligible in that narrow window (e.g. one Result tombstoned) renders as if freshly validated.
  evidence: Same "theoretical today" shape as the two entries above for Stories 3.1/3.2 -- no current mutation/delete path exists that could change a completed job's status/tombstone state mid-request (repo-wide, no cross-call transaction/snapshot mechanism anywhere in `BacktestRepository`), so the window is theoretical today. Not fixed here for the same reason: no in-request mutation path exists to actually trigger it. Revisit together with the other two entries if a future admin/delete-Result feature is added.

## Deferred from: code review of spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract (2026-08-21)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract.md`
  summary: Strategy Skill import-graph validation (`skill_discovery.py`) only inspects `import` statements; `eval`/`exec`/`compile`/`open`/attribute-based `__import__` obfuscation bypasses it entirely, and the literal `__import__`/`import_module` name match is trivially defeated or produces false positives on an unrelated method sharing that name.
  evidence: This is the first production import-graph validator this codebase has ever had; the epic's own AC enumerates "dynamic imports" specifically (which this catches) without requiring full obfuscation-proofing against a deliberately malicious runtime author. Not fixed here: closing every obfuscation vector is an open-ended hardening problem, not a defect relative to this story's stated scope. Revisit if third-party (non-bundled) Strategy Skills become a real threat model rather than the six trusted bundled `rtly-backtest-*` Skills.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract.md`
  summary: The repository's stage-write path (`set_strategy_job_current_stage` or equivalent) doesn't enforce forward-only stage-sequence monotonicity — nothing stops a caller from writing an out-of-order or regressed `current_stage` value.
  evidence: Today only the trusted stub Bootstrap/Preparation workers call this API, walking their stage sequence in a single deterministic order, so it's unexploitable now. Not fixed here since it needs no immediate caller to violate it. Revisit before Stories 4.3/4.6 land their real Bootstrap/Preparation workers, which will be the first untrusted-relative-to-this-guarantee callers.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract.md`
  summary: `_create_stage_job` (or equivalent job-creation path) validates that a supplied `parent_job_id` exists but not that it isn't tombstoned (`deleted_at` set), of a sensible type, or self-referential.
  evidence: No current caller supplies an untrusted or attacker-influenced `parent_job_id`; narrow edge case with no triggering path today. Revisit if job creation is ever exposed to less-trusted input.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract.md`
  summary: `StrategyJobService`'s lease/heartbeat construction doesn't validate a non-positive `lease_ttl_seconds` or reject a corrupted persisted lease row at load time.
  evidence: Requires active misconfiguration (a bad constructor argument or hand-edited DB row) to trigger; no code path produces either today. Revisit if lease TTL ever becomes externally configurable (env var, settings UI) rather than a fixed constant.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-4-2-four-activity-schema-and-strategy-skill-contract.md`
  summary: `MarketView.require_selected`'s docstring claims to be the choke point every Strategy signal passes through, but the engine's actual out-of-universe rejection runs through a separate `SkippedSignalEventV1(INELIGIBLE_SECURITY)` path that never calls `require_selected`.
  evidence: Behavior meets the story's AC either way (an unselected-security signal is explicitly rejected before execution, not silently dropped — just via a different, pre-existing engine-level mechanism than the one the docstring describes). Not fixed here as a functional matter; the docstring should eventually be corrected to describe the real two-path design so a future reader isn't misled about where the actual guarantee lives.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-4-4-4-5-bootstrap-readiness-universe-selection.md`
  summary: `_evaluate_coverage` in `StrategyReadinessService` catches all exceptions from `snapshot_coverage()` and returns `INTEGRITY_ERROR`, which could mask transient database errors (e.g., `sqlite3.OperationalError` for a locked database) as permanent integrity failures.
  evidence: The catch-all is intentional per the design decision for robustness, but a transient database lock would be reported as a permanent integrity error, potentially confusing operators. Consider catching only `BacktestIntegrityError` and logging unexpected exceptions so operators can distinguish transient vs. permanent failures.

## Deferred from: code review of 4-6-2-make-bootstrap-submission-idempotent (2026-08-22)

- Bootstrap submission parent validation still inherits the generic stage-job rule that checks only whether `parent_job_id` exists. It does not constrain parent type, require terminal/non-deleted state, or define Bootstrap lineage semantics. No authorized setup-route input supplies a parent today, so this is pre-existing lifecycle hardening rather than a Story 4.6.2 user-path defect; resolve it when Bootstrap restart/lineage behavior is specified.

## Deferred from: code review of spec-4-6-prepare-the-evidence-needed-for-a-backtest.md (2026-08-24)

- source_spec: `spec-4-6-prepare-the-evidence-needed-for-a-backtest.md`
  status: resolved-2026-08-24
  summary: Bootstrap qualification failure now provides a direct fresh setup submission and exposes the safe recorded provider reason.
  evidence: `StrategyBootstrapService._run_qualification()` retains `latest.failure_reason`; the failed Bootstrap Activity renders “Try setup again” with a new idempotency key.
# Deferred from: code review of spec-gh-288-289-strategy-manager-activity-and-migration (2026-08-24)

- source_spec: `spec-gh-288-289-strategy-manager-activity-and-migration.md`
  summary: Shared activity polling has no out-of-order-response guard, and stale lifecycle action versions return generic 409 text.
  evidence: These behaviors predate Bootstrap activity rendering and affect the existing initialization/backtest activity routes as well.

## Deferred from: code review of spec-gh-291-realised-pnl-collapse-summary (2026-08-24)

- source_spec: `spec-gh-291-realised-pnl-collapse-summary.md`
  summary: A resolved GBP P&L that rounds to negative zero can render as `+£-0.00`, producing contradictory sign, colour, and win classification.
  evidence: `_round2` preserves a float negative-zero sign, while the pre-existing realised-P&L template uses `>= 0` for both positive styling and explicit `+` rendering. The issue predates this story's win/loss count and needs a shared money-display/normalisation decision rather than a narrow review patch.

## Deferred from: code review of spec-gh-305-whole-universe-selection (2026-08-25)

- source_spec: `spec-gh-305-whole-universe-selection.md`
  summary: `_configuration_context()` never sets `selected_security_ids` in its returned template context (only the standalone `universe_selector` GET route does), so any POST validation-error 422 re-render of `_strategy_configuration.html` silently blanks the manual security picker -- no checkbox stays checked, no chips render, "0 selected" is shown -- even though the user's actual submitted `security_ids` are still known server-side.
  evidence: `grep -n "selected_security_ids" app/api/routes/strategy_manager.py` shows exactly one assignment, inside `universe_selector` (the htmx search-partial route), never inside `_configuration_context`. This predates the whole-universe toggle work (Story 4.5's original manual-only picker had the same gap) and is out of that story's stated scope; surfaced incidentally by an adversarial review of the whole-universe diff.

## Deferred from: code review of spec-4-9-complete-the-first-backtest-from-a-clean-checkout (2026-08-25)

- source_spec: `spec-4-9-complete-the-first-backtest-from-a-clean-checkout.md`
  summary: The onboarding doc's Troubleshooting table has no row for resubmitting Setup while a Bootstrap job is already Queued/Running, and no row for an auth session/token expiring mid-journey (between page load and POST, or during Activity polling).
  evidence: Story 4.6.2 already makes Bootstrap submission idempotent (`StrategyBootstrapService.start_setup` / `StrategyJobService.enqueue_bootstrap`), so the behavior exists, but the doc only documents the already-completed no-op case, not the still-running case; a 401/403 mid-flow has no documented recovery step. Both are documentation completeness gaps, not functional bugs, and out of this story's core AC (which is about removing stale/false instructions, not exhaustively cataloging every UI edge case).
- source_spec: `spec-4-9-complete-the-first-backtest-from-a-clean-checkout.md`
  summary: The FX-unavailable error message template (`Pinned historical FX evidence is unavailable for '<security-id>' as of YYYY-MM-01.`) quoted in the onboarding doc is not asserted by any test, so the doc's exact wording can silently drift from the code.
  evidence: Neither the pre-existing Backtest launch tests nor the new `test_strategy_manager_clean_checkout_journey.py` assert this literal string; it was carried forward unchanged from the prior onboarding doc revision. Fixing it would mean adding a regression test tying doc prose to a launch-validation error message, which is broader than this story's scope (replacing stale bootstrap instructions and adding one composed journey test).
- source_spec: `spec-4-9-complete-the-first-backtest-from-a-clean-checkout.md`
  summary: The doc's "Set up Strategy Manager" section describes Setup's repeat-submission no-op only for "already produced a compatible active profile," without saying what happens if the active profile has since become incompatible (e.g., roster drift) between an initial Setup and a repeat submission.
  evidence: `StrategyBootstrapService.start_setup()` docstring says "if a compatible active profile already exists, this is a no-op," implying an incompatible active profile takes a different path, but this diff's doc prose doesn't disambiguate the two cases. Pre-existing ambiguity in the source docstring, not introduced by this story's rewrite.

## Deferred from: code review of spec-gh-312-canslim-tile-breakdown (2026-08-25)

- source_spec: `spec-gh-312-canslim-tile-breakdown.md`
  summary: `AlertAgent.check_positions`'s `stock_map = {s.ticker: s for s in stocks}` (and the pre-existing, structurally identical `stock_map` param on `check_portfolio_stops`) silently keeps whichever `StockRecord` comes last when a run's `stocks` list contains two entries for the same ticker, with no warning or dedup guard.
  evidence: This dict-comprehension pattern already existed as `price_map = {s.ticker: s.price for s in stocks}` before this story (same last-write-wins collision behavior on a duplicate ticker); this story only widened what gets silently kept per ticker from a bare price to the full `StockRecord` (including its `CANSLIMScore`), raising the practical stakes of a collision without introducing the collision risk itself. No current caller produces duplicate tickers within one run's scan output. Revisit if a future data-source change can plausibly emit duplicate ticker rows in a single run.

## Deferred from: code review of spec-gh-310-put-interactive-investor-behind-a-versioned-import-contract (2026-08-26)

- source_spec: `spec-gh-310-put-interactive-investor-behind-a-versioned-import-contract.md`
  summary: `PortfolioImportContractV1`'s `encoding` and `delimiter` fields are schema-validated and shipped in the II contract JSON, but `import_sipp` still decodes CSV bytes and builds `csv.DictReader` with hardcoded `utf-8-sig`/comma *before* contract detection ever runs (detection needs a parsed header row, so contract-driven decode is a genuine chicken-and-egg problem this story didn't resolve). Likewise `priority` and `account_type_ids` are schema-validated and populated in the shipped contract but never consulted by `ContractRegistry.detect`'s tie-break (which uses only required-column count).
  evidence: Confirmed via `grep` — no code path reads `contract.encoding`, `contract.delimiter`, `contract.priority`, or `contract.account_type_ids` outside the schema itself. Zero behavioral impact today: exactly one contract exists and its values match the current hardcoded defaults. This becomes a real risk the moment Story 3.4 (add IG) ships a contract with a genuinely different encoding/delimiter, or a second contract creates an actual tie needing `priority` to break it — must be resolved before or during that story, not deferred again.
- source_spec: `spec-gh-310-put-interactive-investor-behind-a-versioned-import-contract.md`
  summary: `_detect_reconciliation_issues` (`app/agents/trader/trader_agent.py`) reads raw literal Interactive-Investor column names (`"Date"`, `"Debit"`, `"Credit"`, `"Running Balance"`, `"Reference"`) directly off `rows`, not the new canonical-keyed `normalized_rows` the rest of `import_sipp` now uses — a second provider contract that names or aliases these columns differently would make reconciliation-issue detection silently see blanks for every row (`.get(literal, "")` defaults to empty, not an error) rather than fail loudly.
  evidence: Deliberate, spec-compliant deviation: this function is independently unit-tested against raw-column-keyed dicts in `tests/test_trader_agent.py` (three tests), and the spec's own first "Always" boundary ("every existing test passes unmodified... that is a signal the refactor changed behavior, not a spec update") takes priority over its later, looser "Never" wording about this function's input source changing. Confirmed via code comment already in place at the call site. Must be resolved (either by making this function contract-aware or by normalizing its input too, updating its three existing tests deliberately) before or during Story 3.4.
- source_spec: `spec-gh-310-put-interactive-investor-behind-a-versioned-import-contract.md`
  summary: `ContractRegistry.detect`'s "missing required columns" error message (the non-ambiguous, non-empty-registry zero-match path) picks `best_missing` from whichever non-matching contract the directory-glob-ordered loop visits first when multiple contracts tie on "fewest missing columns" — the other equally-close candidate's missing-column list is silently discarded.
  evidence: Confirmed in `contract_registry.py`'s `detect`: `best_missing` is updated only on strict `<` comparison, so a genuine tie keeps whichever contract was scored first. Unreachable today with one shipped contract (no second candidate exists to tie against), so no test exercises it. Revisit once a second contract exists and this tie can actually occur — likely needs a defined, tested tie-break rule (e.g., contract `priority`, alphabetical `contract_id`) rather than incidental glob order.

## Deferred from: code review of spec-gh-311-persist-import-receipt-and-contract-provenance (2026-08-26)

- source_spec: `spec-gh-311-persist-import-receipt-and-contract-provenance.md`
  summary: `canonical_row_digest` hashes the row's *pre-canonicalization* normalized view (`normalized_rows[idx]`, before `_to_iso_date` conversion, ticker canonicalization, and Money-amount parsing), not the fully-resolved values the row actually writes. Two rows that resolve to the identical trade/cash-flow (same `idempotency_key`) are not guaranteed to produce the same `canonical_row_digest`, and vice versa.
  evidence: Confirmed `row_digests = [canonical_row_digest(row) for row in normalized_rows]` runs before the per-row date/ticker/amount resolution loop. Not fixed here: doing so properly requires moving digest computation into the resolution loop itself (per-row, after `date`/`symbol`/parsed amounts are known), a more invasive restructuring than this review pass's scope. No AC in this story requires digest-equals-idempotency-key equivalence — the digest's stated purpose (provider-neutral, one-way, no raw content persisted) is still satisfied. Revisit if a future consumer needs the digest to double as a content-based dedup signal.
- source_spec: `spec-gh-311-persist-import-receipt-and-contract-provenance.md`
  summary: `portfolio_import_receipts.account_type_id` is populated as `",".join(contract.account_type_ids) or None` — a lossy, unescaped comma-join of the *contract's supported* account types, not the specific account type the user selected for this import (no such per-request selection exists yet; that's Story 3.3's UI work).
  evidence: Confirmed no caller passes a per-request account-type selection into `import_sipp` today. Harmless for the single shipped II contract (`account_type_ids: ["sipp"]`, a one-element list so the join is exactly that one value) — becomes actively wrong/ambiguous once either a contract supports multiple account types or Story 3.3 wires through a real user selection that should be threaded to the receipt instead of the contract's full supported set. Must be resolved when Story 3.3 adds the actual provider/account-type selection UI.

- Review Triage Log for this spec found and fixed (as `patch`, same pass) a verified, real regression: `portfolio_import_rows.trade_id`/`cash_flow_id` referenced `trades(id)`/`cash_flows(id)` with no `ON DELETE` clause under `PRAGMA foreign_keys = ON`, so deleting any SIPP-imported trade (`delete_trade`, or bulk via `delete_portfolio`) raised `sqlite3.IntegrityError` the moment a lineage row referenced it. Fixed with `ON DELETE SET NULL` on both FKs (verified live before and after) and covered by two new regression tests. Recorded here only as a note that this was found and fixed in-pass, not left outstanding.

## Deferred from: code review of spec-gh-308-select-and-validate-provider-account-imports (2026-08-26)

- source_spec: `spec-gh-308-select-and-validate-provider-account-imports.md`
  summary: `_MAX_FAILED_ROWS_LISTED` (200, the full on-page issue list) and `_MAX_ISSUE_DETAILS` (5, the notification-body/prose-summary cap) are two independent, uncoordinated magic-number caps over overlapping "row problem" data, each with its own "+N more" truncation logic.
  evidence: Confirmed both constants exist for genuinely different consumers (full page display vs. notification body prose) with no shared derivation. Not a bug today -- both correctly bound their own output -- but a maintainability risk: changing one cap's wording/behavior without checking the other is an easy future mistake. Revisit if a third similar cap is ever added, or if the two drift in an actual review finding.
- source_spec: `spec-gh-308-select-and-validate-provider-account-imports.md`
  summary: `import_failed_rows` (the rejected-plan issue list rendered on `_portfolio.html`) is shown verbatim, unlike the new `preview_rows` table on the same page, which truncates free-text/general cells to 40/60 chars specifically because it can carry "real user financial data" (this story's own stated rationale).
  evidence: Confirmed `failed_rows` entries are prose strings like `"row REF: field code: detail"` built by pre-existing code in `TraderAgent.import_sipp` (unchanged by this story), where `detail` can embed the actual malformed cell value and `REF` can be a free-text Reference field. This story only widened how many are shown (5 in the notification body → up to 200 on the page), it did not introduce the lack of redaction. Low practical severity today: `POST /import-sipp` requires `require_local_or_token` and this is a single-user local finance tool, not a multi-tenant service. Revisit if the failed-rows list ever needs the same redaction policy as the preview table, or if this app ever grows a broader/shared-access audience.

## Deferred from: code review of spec-gh-309-add-ig-as-a-contract-only-provider (2026-08-26)

- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: The `reject_unless_column_equals` capability makes a single out-of-scope row (e.g. one CFD/spread-bet activity line) reject an IG import in full, including every legitimate share-dealing row in the same export.
  evidence: Confirmed this routes through the same all-or-nothing `failed_rows` channel every other row failure already uses (matches this app's existing #210 all-or-nothing-commit philosophy for every provider, not a new IG-specific gap). Real product-fit limitation for any IG customer whose account has ever done CFD/spread-bet trading: they cannot import their share-dealing history without hand-editing the CSV first. A "skip only the unsupported rows, import the rest" mode would need to relax the all-or-nothing rule app-wide, which is a separately scoped feature decision, not something to slip into this story.
- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: `TextExtractionRule.pattern` has no complexity/ReDoS safeguard, even though it runs a contract-declared regex against untrusted uploaded CSV free text.
  evidence: IG's own shipped pattern is simple and benign (verified). The generic capability itself, though, has no bound on pattern complexity, nested/ambiguous quantifiers, or a per-row timeout before `re.match` runs. Not exploitable today (only one such contract exists, authored by the maintainer), but a latent risk for any future contract author. Revisit if/when a third contract introduces a new `text_extraction` pattern -- add either a complexity heuristic at contract-load time or a timeout wrapper around the match call.
- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: Mixing a running-balance provider (e.g. II) and a non-running-balance provider (e.g. IG) into the *same* `portfolio_id` cannot produce a jointly-coherent cash balance, even after review pass 1's fix ensures the additive delta is never silently dropped.
  evidence: The legacy single-value cash-balance slot (`account_state cash_balance:{pid}`) was designed for one authoritative source of truth per portfolio. A portfolio realistically maps to one broker account, so this scenario is expected to be rare/unusual by construction, not a common real-world case this story needs to fully reconcile. If it does occur, a later dated II snapshot will overwrite whatever number IG's deltas had accumulated on top of, without knowing to "re-add" them. Revisit only if real usage shows users deliberately importing multiple providers into one portfolio.
- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: `tests/test_no_provider_conditionals.py`'s architecture guard is a simple case-insensitive literal-substring regex, not a full AST analysis -- a determined future contributor could still write a `provider_id`-comparison that evades it (e.g. via string concatenation or an indirect constant).
  evidence: Confirmed by design (the spec explicitly calls for "a simple regex-based grep-in-test, not a full AST analysis" to keep the check cheap and maintainable). Nothing in this codebase's existing style does that kind of obfuscation today. Revisit only if the guard is ever observed to miss a real violation in practice. (Review pass 2 broadened it to also catch `provider_id in (...)` membership checks.)
- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: The cash-balance fallback's new non-GBP-currency rejection is effectively dead code for the shipped IG contract, and IG's `Currency`/`CurrencyIsoCode` columns are declared as optional but never read anywhere.
  evidence: Confirmed `signed_amount_column` always emits a currency-agnostic plain numeric string per its own design (mirrors every other canonical field's string-typed contract), so a row sourced from IG's `PL Amount` can never itself carry non-GBP currency evidence -- the only test exercising the rejection path uses a hand-built synthetic contract, not `ig_v1.json`. IG genuinely supports non-GBP share dealing in reality, so a real (non-fabricated) multi-currency IG account would have its cash balance silently computed as if every row were GBP, with zero protection. Not fixed here: doing so correctly requires real evidence of how IG's export represents a non-GBP trade's currency (its own real column, an FX-converted derivative of "Converted at X", etc.) -- exactly the kind of "never infer a financial import format from an undocumented sample" case this story's own guidance warns against. Revisit only with a real (sanitized) non-GBP IG export sample to design against.
- source_spec: `spec-gh-309-add-ig-as-a-contract-only-provider.md`
  summary: `RejectedRowValueRule.allowed_value` is a single hardcoded string, so a future IG export variant using a differently-formatted (but equally valid) share-dealing marker value would need a whole new contract version rather than an updated allow-list.
  evidence: Confirmed the schema has no way to express "any of these values are acceptable." Low priority, YAGNI until a second real value variant is actually observed -- speculative multi-value support now would be unvalidated against any real data.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-345-cache-whalewisdom-heat-map.md`
  summary: When ExtractionAgent serves a stale WhaleWisdom heat-map cache after a failed refresh, it still overwrites ww_context.json and the extraction_results.json WW group with the stale payload stamped with today's date, so file consumers see no staleness signal (only source_health carries it).
  evidence: `_fetch_heat_map` stale_cache path returns cached children; `run()` unconditionally calls `_save_ww_context(ranked)` and `_update_results_with_sources(...)` which key on `datetime.today()`.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-345-cache-whalewisdom-heat-map.md`
  summary: The heat-map cache file has no cross-process lock; a CLI pipeline run and the web-server pipeline run in parallel could have the slower one overwrite a newer entry.
  evidence: `heat_map_cache.store()` does a bare tmp-write + `os.replace`; `_run_lock` in pipeline_service is process-local and the orchestrator runs as a subprocess. `os.replace` keeps writes atomic (no corruption); spec Block-If already flags richer storage as out of scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: Deterministic-fallback narrative runs (no Anthropic key) re-save with a fresh `generated_at` every pipeline run, churning the banner timestamp even though the deterministic text never changes.
  evidence: `resolve_market_narrative` always calls `save_market_narrative` on the non-reuse path, and `reusable_narrative` unconditionally rejects `from_fallback` narratives, so a keyless deployment never benefits from the cache.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: A transient news/Claude outage overwrites a good persisted LLM narrative with a `from_fallback` one, forcing an extra regeneration once the outage clears even though the facts never moved.
  evidence: The fallback path stamps `from_fallback=True` and saves unconditionally; the next run cannot reuse it (fallback refusal) so it regenerates. Spec's I/O matrix mandates the overwrite.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: `NARRATIVE_VERSION` is a hand-maintained constant with no enforcement — editing the Claude system prompt or a narrative builder without bumping it serves stale cached prose for up to `NARRATIVE_MAX_AGE` (24h) after each unchanged run.
  evidence: Nothing ties the prompt-file / builder source to the version; a test asserting a bump on prompt change, or folding a hash of the prompt file into the digest, would close it.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: The digest includes `breadth.as_of` but deliberately excludes `snapshot.as_of`; the asymmetry is safe today (breadth `as_of` is a date string, stable intraday) but a future intraday-timestamped breadth feed would defeat the cache.
  evidence: `narrative_input_digest` canonical dict includes `breadth.as_of`; `snapshot.as_of` is omitted with a stated rationale that also applies to breadth.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: `breadth.retrieval_source` is in the digest and flips from "fetched"/"source" on the first pipeline run of the day to "cached" on the second once the #343 daily breadth cache is populated, forcing exactly one extra narrative regeneration (news fetch + Claude call) per day at the run1->run2 transition.
  evidence: `narrative_input_digest` includes `breadth.retrieval_source`; it was added in review pass 2 because it renders verbatim in `_breadth_bullet` and the Claude prompt. Excluding it would trade a small provenance-phrase inaccuracy in a reused narrative for eliminating that daily regen.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: The digest hashes the full `congress` and `portfolio_weights` lists although the prose only renders `[:_TOP_N]` (3), so a change to a rank-4+ entry invalidates the cache and spends an LLM call with no possible change to rendered output.
  evidence: `_congress_bullet`/`_portfolio_bullet` slice to `_TOP_N`; `narrative_input_digest` iterates the whole list. `shares` genuinely cannot be truncated (`_myb_bullet` scans all of it), but congress/weights could be sliced in the digest.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-377-narrative-input-digest.md`
  summary: When `narrative_input_digest` returns `None` (non-finite float from a glitchy feed), `resolve_market_narrative` regenerates and overwrites the previously-good narrative + digest on disk with `input_digest=""`, so the next healthy run cannot reuse and must regenerate a second time.
  evidence: The `digest is None` path still calls `save_market_narrative(stamped)` with an empty digest. Spec I/O matrix currently mandates this ("no digest stamped; regenerate"); leaving the existing file untouched when the digest cannot be computed would avoid the double regen.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-367-trade-log-security-names.md`
  summary: When a Trade Log row's security_id does not resolve in the pinned roster, the "Unknown security" label is identical across rows and the only distinguishing datum (the GUID) is hidden in a collapsed per-row `<details>`, so an operator cannot tell unresolved rows apart at a glance.
  evidence: `resolve_security_label` returns the bare constant `UNRESOLVED_SECURITY_LABEL` on a miss; the spec's read-only I/O matrix fixes that as "the fixed fallback string". A future change could append a short id to the fallback label.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-367-trade-log-security-names.md`
  summary: The Trade Log renders one `<details>/<summary>Audit detail</summary>` disclosure per row; a 200-event backtest produces 200 disclosure widgets in one column, all with the same non-unique accessible name.
  evidence: `_backtest_result.html` and `_comparison.html` Security cells. The `<details>` pattern is mandated by the spec intent-contract; a column-level toggle or per-row `aria-label` would scale better.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-387-scanner-market-regime.md`
  summary: The market-regime banner has no explicit max-age/"as of" treatment; if the scan cron breaks it keeps asserting a regime state with no visible age.
  evidence: `load_market_regime()` reads the snapshot with no staleness check; `generated_at` is persisted but never displayed. `market_narrative` enforces `NARRATIVE_MAX_AGE`. Same-run coupling to the analysis artifact (whose freshness IS shown on the /pipeline-status bar, #71) makes this lower priority, but a dedicated cutoff or an "as of {date}" line is a real UX gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-387-scanner-market-regime.md`
  summary: The existing `#market-narrative` banner sets Bootstrap `.border-0` (`border:0 !important`) alongside an inline non-important `border`, so its intended coloured outline never renders.
  evidence: `_stock_scanner.html` market-narrative div, pre-existing. #387's new `_market_regime.html` avoids the conflict; the narrative block should be brought in line.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-388-market-regime-entry-filter.md`
  summary: `app/services/backtest/regime_filter.py` is simulation-affecting shared logic (when the filter is enabled) that no semantic digest covers — it is not in `_MARKET_VIEW_ALLOWLIST` or `_LEDGER_ACTION_METRICS_ALLOWLIST`, so editing its SMA math changes enabled-Run output while `execution_contract_digest` stays identical and `is_comparable` still declares two such Runs comparable.
  evidence: #388 explicitly requires `execution_contract_digest` to stay unchanged (so the pre-#388 result corpus stays comparable). Adding the module to a contract allowlist would move the digest for every Run, filter-on or off. Revisit once the filter sees real use — perhaps a filter-specific contribution to the replay digest only when enabled.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-388-market-regime-entry-filter.md`
  summary: Enabling the filter with an unset, mistyped, or non-selected benchmark silently produces an all-cash backtest — no launch-time validation that an enabled filter's `regime_filter_benchmark_security_id` is one of the Run's selected securities.
  evidence: `backtest_launch_service` / `strategy_manager` were out of scope for #388; the "must be one of the Run's selected securities" rule lives only in the parameter description string. Runtime fail-closed is per spec, but the zero-entry outcome is indistinguishable from a genuinely bearish regime.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-388-market-regime-entry-filter.md`
  summary: An enabled filter whose benchmark has fewer than `regime_filter_ma_length` (default 200) sessions of history — early in a Run, or a persistently-erroring `price_history` — suppresses entries silently, with no diagnostic distinguishing "insufficient warm-up" or "view error" from a real risk-off regime.
  evidence: strategy-runtime modules cannot log; fail-closed is silent by the codebase's own convention. A run-level warning surface for "regime filter suppressed N sessions for reason X" would help users trust the output.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-388-market-regime-entry-filter.md`
  summary: A regime-gated Run and an ungated Run are declared comparable by `is_comparable` (params are excluded from `execution_contract_digest` by design), and nothing on the comparison surface flags that one leaderboard row was regime-filtered.
  evidence: consistent with how every other parameter is treated (AD-19), but the regime filter reshapes the return distribution far more than a lookback tweak. Consider surfacing `regime_filter_enabled` in the compare UI.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-399-backtest-config-wizard.md`
  summary: `_universe_selector.html`'s inline script re-binds listeners and re-queries the DOM on every HTMX swap with no idempotency guard.
  evidence: Same non-idempotent inline-script pattern that was fixed in the wizard script (guarded via `document.body.dataset.smWizardBound`); the universe selector partial swaps frequently (search-as-you-type, whole-universe toggle) so handlers/closures accumulate over a session.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-400-declutter-strategy-parameters.md`
  summary: Editing strategy parameters, switching to another strategy, then back (A→B→A) silently discards A's edited parameter values.
  evidence: The strategy radio's `hx-include` deliberately carries only period/capital, not `param__*`; the fields partial refetches at defaults on every strategy change. Pre-existing, but #400's "N parameters · all defaults" summary now reassures the user nothing changed after the loss.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-400-declutter-strategy-parameters.md`
  summary: The parameter disclosure `<summary>` shows the total parameter count, but the `<details>` only contains the non-required subset — misleading for strategies with several required params plus the 3 host-injected regime_filter_* optionals.
  evidence: `_strategy_configuration_fields.html` renders `{{ param_count }} {{ param_word }}` (total) inside `<summary>`; real strategies via `skill_discovery.COMMON_BACKTEST_STRATEGY_PARAMETERS` always have ≥3 optional params. Both review layers flagged the count/contents mismatch; a fix needs a product decision (total vs disclosed count) constrained by the spec's frozen I/O matrix.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-410-declutter-backtest-results-list.md`
  summary: The backtest results list labels each row "Attempt N" from `job.enqueue_seq`, which is a global counter across all strategy-job types, not a per-strategy attempt index — a user's first backtest can render as "Attempt 42".
  evidence: `enqueue_seq` is assigned `COALESCE(MAX(enqueue_seq),0)+1 FROM strategy_jobs` for bootstrap/initialization/preparation/backtest jobs alike (`backtest_repo.py` ~L2119/2220/2337/2393). #410 kept the pre-existing "attempt N" wording and promoted it to the row's primary link label; a true per-strategy attempt number would need a query change that #410's scope forbade.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-418-refresh-freshness-affordance.md`
  summary: Terminal pipeline runs now give no in-page confirmation — a `partial` run's `status.error_summary`, the "Pipeline complete/skipped" summary line, and the post-run stage breakdown all rendered only in the bar, which is now running-only.
  evidence: `_pipeline_status.html` gates the whole bar on `status.state == 'running'`; the breakdown toast fires only when `latest_attempt_error` (FAILED only) or an unhealthy source is present, so `complete`, `skipped`, and `partial`-without-source-issues runs surface nothing at all. The spec pinned the toast block as unchanged, so widening it was out of scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-418-refresh-freshness-affordance.md`
  summary: `Freshness.diagnostic` (clock skew / naive-timestamp warnings) is still never rendered, so a future-dated artifact displays as confidently "fresh" from what is now the product's only freshness surface.
  evidence: `freshness_service.calculate_freshness` computes a diagnostic that no template reads; the spec's "Never" list froze the `calculate_freshness` contract and `build_freshness_context()`'s return shape, and surfacing it needs a copy decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-418-refresh-freshness-affordance.md`
  summary: `GET /` went from a zero-I/O shell render to synchronously parsing the whole `analysis_results.json` plus the status artifact on the event loop, and the load-triggered `/pipeline-status` fetch immediately re-does the same work.
  evidence: `build_freshness_context()` → `read_analysis_artifact_meta()` does `json.loads(path.read_text())` over every record to pull two fields, called directly inside `async def index`. Consistent with how `build_stock_scanner_context` is already called from async routes, so this is a codebase-wide pattern rather than a #418 defect, but #418 is what put it on the shell route.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-421-portfolio-chart-range.md`
  summary: The portfolio chart range selector renders on the legacy single-portfolio `portfolio_value.csv` view (`_load_portfolio_history` with `portfolio_id is None`), where `range_key` is ignored, so the buttons appear active but never filter.
  evidence: Post-#147 every UI path resolves an `active_id` so the `None` branch is effectively unreachable from the live app, but the selector markup is unconditional in `_portfolio_chart.html`; a deployment still on the CSV path would show non-functional controls.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-421-portfolio-chart-range.md`
  summary: `chart_has_history` issues a dedicated `snapshot_history(portfolio_id, limit=2)` query on every portfolio render and every chart-fragment render, on top of the history load already performed.
  evidence: Both reviewers flagged the extra round-trip; it exists because the range-filtered history load cannot answer "does any history exist at all" once the window is empty. Could be folded into a single query returning both the windowed series and an any-rows flag.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-440-assign-portfolio-strategy.md`
  summary: `StrategyAssignmentService.freshness()` stats/reads `ANALYSIS_JSON` and the assignment repo on every portfolio partial render, even when no assignment exists.
  evidence: `app/services/portfolio_service.py` calls `assignment_service.freshness()`/`assignment_view()` inside `default_portfolio_context`; the artifact is a small file so the cost is minor today, but it is per-render I/O on the hottest partial — cache by (mtime, portfolio revision) if profiling ever shows it.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-441-portfolio-recommendations.md`
  summary: Strategy runtime evaluation runs synchronously inside the async recommendations route with no execution timeout — a slow or looping runtime freezes the event loop.
  evidence: `portfolio_recommendations()` calls `recommend()` directly, which does filesystem discovery, `spec_from_file_location`/`exec_module` of strategy code, pandas work and SQLite reads on the event-loop thread; contrast plan 012's subprocess timeout for the pipeline. Off-loading to a worker with a bounded timeout is a cross-cutting decision affecting all strategy execution paths.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-442-portfolio-recommendation-email.md`
  summary: Per-portfolio recommendation emails are sent synchronously inside the pipeline's EXPORT stage — N portfolios × SMTP round-trips (starttls + login each) sit in the pipeline's critical path.
  evidence: `dispatch_recommendation_emails` runs inline before `status_repo.transition(EXPORT, COMPLETE)`; a slow SMTP host delays (or via plan 012's subprocess timeout, times out) the whole pipeline run. Bounding this needs the same off-loop/worker decision as the #441 runtime-evaluation deferral.

## Deferred from: code review of gh-445 strategy-manager full-page fallback (2026-08-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-445-strategy-manager-full-page-fallback.md`
  summary: `page.html` duplicates index.html's asset tags and `strategy-manager.js?v=` cache-bust version with no sync mechanism, so bumping the version in `index.html` leaves directly-opened pages on stale JS.
  evidence: `app/api/templates/page.html` head/body asset list is a verbatim copy of `app/api/templates/index.html` lines 5-14/756/1096; the spec's Never clause forbade extracting shared partials in this story.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-445-strategy-manager-full-page-fallback.md`
  summary: Only strategy-manager routes wrap fragments for direct navigation; sibling `/partials/*` routes in `app/api/routes/views.py` still return bare fragments when opened directly.
  evidence: Spec Never clause scoped the change to strategy-manager renders; the asymmetry is visible if a user direct-opens e.g. `/partials/portfolio`.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-445-strategy-manager-full-page-fallback.md`
  summary: Pre-existing direct-GET tests in `tests/test_strategy_manager_routes.py` (e.g. `test_initialization_uses_plain_copy_and_describes_preparation_history`) now exercise the wrapped full-page path while being named/read as fragment tests; the bare-fragment htmx path is only covered for 2 of 16+ fragments.
  evidence: Those tests send no `HX-Request` header and pass via substring assertions that hold inside the wrapped page; updating them all was judged too much churn for this story.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-445-strategy-manager-full-page-fallback.md`
  summary: Direct-navigation coverage exists for only 2 fragment templates; `_backtest_result.html` (inline parse-time script relying on Chart.js load order) is untested on the wrapped path.
  evidence: New tests cover `_historical_initialization.html` and `_initialization_activity.html` only; the result-page fixture setup is heavier and was deferred.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-454-portfolio-dashboard-layout.md`
  summary: The existing market-value GBP projection uses the default GBP/USD rate whenever a live or cached rate is unavailable, without displaying that fallback provenance on the Portfolio summary card.
  evidence: `portfolio_partial_context()` retains the pre-existing `gbpusd_rate or _DEFAULT_GBPUSD` conversion contract; #454 separates the market-only projection but does not change FX freshness/valuation semantics.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-455-total-portfolio-value-chart.md`
  summary: The chart teardown contract is asserted from rendered JavaScript source but has no browser-level test that performs consecutive htmx fragment replacements with a live Chart.js instance.
  evidence: Existing #421 and #455 tests verify the stable card, fragment endpoints, and destroy-before-create source ordering; none executes Chart.js across repeated swaps, and adding that browser harness is independent of the service-owned total-value projection.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-467-nuanced-breadth-bullet.md`
  summary: `market_breadth._to_pct` accepts the literal strings "nan"/"inf" for the primary `pct_above_200dma` field, yielding a NaN reading that renders "nan%" and aborts `narrative_input_digest` (regenerating the LLM narrative every run).
  evidence: `float("nan")`/`float("inf")` parse successfully and are not caught by the existing `ValueError` fail-soft path; `_quant` then raises `_NonFiniteFloat`. Pre-existing — this pattern predates gh-467, which only added `math.isfinite` guards on the new delta paths.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-467-nuanced-breadth-bullet.md`
  summary: `market_breadth._parse_latest` is not self-contained fail-safe — `latest["Date"]`, `latest["Breadth_Index_Raw"]`, and `datetime.strptime` can all raise; a direct caller not wrapping it (only `fetch_market_breadth` does today) would get an exception instead of `None`.
  evidence: The module/function docstrings imply "parse … or None"; the actual None-on-bad-data contract lives in the caller's `try/except (KeyError, ValueError, TypeError)`.
