# Vcp Specification

> Generated by spec-gen v1.0.0 on 2026-05-10 22:19

## Purpose

Integrates with external VCP (Volatility Contraction Pattern) screener to identify potential
breakout candidates from S&P 500 stocks.

## Requirements

> `agents/scanner/scanner_agent.py`

### Requirement: FetchVCPCandidates

> Implementation: `fetch_vcp_screener_tickers` in `agents\scanner\scanner_agent.py` · confidence: llm

The system SHALL executes external vcp screener subprocess against s&p 500 and returns candidate ticker symbols.

#### Scenario: SuccessfulVCPScreening
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** VCP screener script exists and Node.js is available
- **WHEN** fetch_vcp_screener_tickers is called
- **THEN** Returns list of VCP pattern candidate tickers from S&P 500

#### Scenario: MissingDependencies
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Node.js or VCP script is not available
- **WHEN** fetch_vcp_screener_tickers is called
- **THEN** Returns empty list or raises execution error


## Sub-components

> `VCPScreener` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: PathResolution

> Implements: `Path`

Resolves file system paths for VCP screener script and output files.

#### Requirement: PathConstruction

The system SHALL construct valid path objects from string paths

#### Scenario: ValidPathCreation
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** a valid file path string
- **WHEN** Path constructor is called
- **THEN** a Path object is returned

### Sub-component: ScriptExistenceCheck

> Implements: `_VCP_SCRIPT.exists`

Verifies that the VCP screener script file exists on the file system.

#### Requirement: FileExistenceValidation

The system SHALL return true if vcp script file exists, false otherwise

#### Scenario: ScriptExists
- **GIVEN** VCP script file is present on file system
- **WHEN** exists() is called
- **THEN** True is returned

#### Scenario: ScriptMissing
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** VCP script file is not present
- **WHEN** exists() is called
- **THEN** False is returned

### Sub-component: ExecutableLookup

> Implements: `shutil.which`

Locates executable programs in the system PATH environment.

#### Requirement: ExecutableResolution

The system SHALL return full path to executable if found in path, none otherwise

#### Scenario: ExecutableFound
- **GIVEN** python executable exists in PATH
- **WHEN** which('python') is called
- **THEN** full path to python executable is returned

#### Scenario: ExecutableNotFound
- **GIVEN** specified executable does not exist in PATH
- **WHEN** which() is called
- **THEN** None is returned

### Sub-component: HomeDirectoryResolution

> Implements: `Path.home`

Resolves the current user's home directory path.

#### Requirement: HomePathRetrieval

The system SHALL return path object pointing to user's home directory

#### Scenario: HomeDirectoryAccess
<!-- spec-gen-test: priority=high (auto) -->
- **GIVEN** user has a valid home directory
- **WHEN** Path.home() is called
- **THEN** Path object to home directory is returned

### Sub-component: TemporaryDirectoryManagement

> Implements: `tempfile.TemporaryDirectory`

Creates and manages temporary directories for VCP screener execution.

#### Requirement: TempDirectoryCreation

The system SHALL create a temporary directory that is automatically cleaned up

#### Scenario: TempDirectoryLifecycle
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** temporary directory is needed
- **WHEN** TemporaryDirectory context manager is used
- **THEN** directory is created and cleaned up automatically

### Sub-component: SubprocessExecution

> Implements: `subprocess.run`

Executes the VCP screener script as a subprocess and captures output.

#### Requirement: ScriptExecution

The system SHALL execute vcp screener script and return process result

#### Scenario: SuccessfulExecution
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** valid VCP script and arguments
- **WHEN** subprocess.run is called
- **THEN** process completes with return code 0

#### Scenario: ExecutionFailure
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** invalid script or arguments
- **WHEN** subprocess.run is called
- **THEN** process fails with non-zero return code

### Sub-component: FilePatternMatching

> Implements: `_glob.glob`

Finds files matching specified patterns in the temporary directory.

#### Requirement: PatternMatching

The system SHALL return list of file paths matching the specified glob pattern

#### Scenario: FilesFound
- **GIVEN** files matching pattern exist
- **WHEN** glob() is called with pattern
- **THEN** list of matching file paths is returned

#### Scenario: NoFilesFound
- **GIVEN** no files match the pattern
- **WHEN** glob() is called
- **THEN** empty list is returned

### Sub-component: JsonDeserialization

> Implements: `json.loads`

Parses JSON strings into Python data structures.

#### Requirement: JsonParsing

The system SHALL convert json string to python object

#### Scenario: ValidJsonParsing
- **GIVEN** valid JSON string
- **WHEN** json.loads() is called
- **THEN** corresponding Python object is returned

#### Scenario: InvalidJsonHandling
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** malformed JSON string
- **WHEN** json.loads() is called
- **THEN** JSONDecodeError is raised

### Sub-component: DataExtraction

> Implements: `data.get`

Safely extracts values from dictionary data structures with default fallback.

#### Requirement: SafeValueRetrieval

The system SHALL return value for key if exists, otherwise return default value

#### Scenario: KeyExists
- **GIVEN** dictionary contains the specified key
- **WHEN** get() is called
- **THEN** associated value is returned

#### Scenario: KeyMissing
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** dictionary does not contain the key
- **WHEN** get() is called with default
- **THEN** default value is returned

### Sub-component: HttpRequest

> Implements: `r.get`

Makes HTTP GET requests to external APIs for data retrieval.

#### Requirement: HttpGetRequest

The system SHALL send http get request and return response object

#### Scenario: SuccessfulRequest
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** valid URL and network connectivity
- **WHEN** get() is called
- **THEN** response with status 200 is returned

#### Scenario: RequestFailure
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** invalid URL or network error
- **WHEN** get() is called
- **THEN** appropriate HTTP error response is returned

### Sub-component: SpyContextFetching

> Implements: `_fetch_spy_context`

Retrieves S&P 500 context data for market analysis.

#### Requirement: SpyDataRetrieval

The system SHALL fetch and return s&p 500 context information

#### Scenario: ContextDataAvailable
- **GIVEN** S&P 500 data source is accessible
- **WHEN** _fetch_spy_context() is called
- **THEN** context data is returned

### Sub-component: WatchlistLoading

> Implements: `load_watchlist`

Loads and flattens ticker symbols from extraction results JSON file.

#### Requirement: TickerExtraction

The system SHALL load tickers from json file and return flattened list

#### Scenario: ValidWatchlistFile
- **GIVEN** extraction_results.json exists with ticker data
- **WHEN** load_watchlist() is called
- **THEN** flattened list of unique tickers is returned

#### Scenario: EmptyWatchlistFile
- **GIVEN** extraction_results.json is empty or missing
- **WHEN** load_watchlist() is called
- **THEN** empty list is returned

### Sub-component: VcpScreenerExecution

> Implements: `fetch_vcp_screener_tickers`

Executes VCP screener against S&P 500 stocks and returns candidate symbols.

#### Requirement: VcpScreening

The system SHALL run vcp screener script and return list of candidate ticker symbols

#### Scenario: SuccessfulVcpScreening
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** VCP script exists and FMP API is accessible
- **WHEN** fetch_vcp_screener_tickers() is called
- **THEN** list of VCP candidate tickers is returned

#### Scenario: VcpScreeningFailure
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** VCP script missing or API unavailable
- **WHEN** fetch_vcp_screener_tickers() is called
- **THEN** empty list is returned

### Sub-component: TvScreenerUS

> Implements: `fetch_tv_screener_tickers`

Retrieves US stock tickers from TradingView screener matching Minervini criteria.

#### Requirement: UsTickerScreening

The system SHALL fetch us tickers from nyse/nasdaq matching minervini pre-filter

#### Scenario: UsScreeningSuccess
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** TradingView screener is accessible
- **WHEN** fetch_tv_screener_tickers() is called
- **THEN** list of US tickers matching criteria is returned

#### Scenario: UsScreeningWithLimit
- **GIVEN** max_rows parameter is specified
- **WHEN** fetch_tv_screener_tickers(100) is called
- **THEN** at most 100 tickers are returned

### Sub-component: TvScreenerUK

> Implements: `fetch_tv_screener_tickers_uk`

Retrieves UK stock tickers from TradingView screener matching Minervini criteria.

#### Requirement: UkTickerScreening

The system SHALL fetch uk lse tickers matching minervini pre-filter

#### Scenario: UkScreeningSuccess
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** TradingView UK screener is accessible
- **WHEN** fetch_tv_screener_tickers_uk() is called
- **THEN** list of UK tickers matching criteria is returned

#### Scenario: UkScreeningWithLimit
- **GIVEN** max_rows parameter is specified
- **WHEN** fetch_tv_screener_tickers_uk(50) is called
- **THEN** at most 50 UK tickers are returned

### Sub-component: ResultsMerging

> Implements: `_merge_results`

Combines ticker lists from multiple screening sources into unified result set.

#### Requirement: TickerListMerging

The system SHALL merge multiple ticker lists while removing duplicates

#### Scenario: MultipleSources
- **GIVEN** ticker lists from VCP, US, and UK screeners
- **WHEN** _merge_results() is called
- **THEN** unified list with unique tickers is returned

#### Scenario: DuplicateRemoval
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** overlapping tickers across sources
- **WHEN** _merge_results() is called
- **THEN** duplicates are removed from final list

### Sub-component: ScanSourceIdentification

> Implements: `self._scan_source`

Identifies the source of each ticker for tracking and attribution.

#### Requirement: SourceAttribution

The system SHALL identify and return the source of ticker data

#### Scenario: SourceIdentification
- **GIVEN** ticker from specific screening source
- **WHEN** _scan_source() is called
- **THEN** appropriate source identifier is returned

### Sub-component: StringEndsWith

> Implements: `t.endswith`

Checks if ticker string ends with specific suffix pattern.

#### Requirement: SuffixValidation

The system SHALL return true if string ends with specified suffix

#### Scenario: SuffixMatch
- **GIVEN** ticker ending with '.L'
- **WHEN** endswith('.L') is called
- **THEN** True is returned

> `skills/vcp-screener/scripts/calculators/vcp_pattern_calculator.py`

### Requirement: CalculateVCPPattern

> Implementation: `calculate_vcp_pattern` in `skills\vcp-screener\scripts\calculators\vcp_pattern_calculator.py` · confidence: llm

The system SHALL analyzes price data to detect vcp patterns by identifying swing points, building contractions, and validating pattern criteria.

#### Scenario: ValidVCPPatternDetected
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Price data with proper contraction sequence
- **WHEN** calculateVCPPattern is called
- **THEN** Returns positive VCP score and valid pattern flag

#### Scenario: InvalidPatternInsufficientContractions
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Price data with less than required contractions
- **WHEN** calculateVCPPattern is called
- **THEN** Returns zero score and invalid pattern flag

#### Scenario: WideAndLooseFinalContraction
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Final contraction exceeds volatility threshold
- **WHEN** calculateVCPPattern is called
- **THEN** Pattern marked as wide-and-loose in validation

### Requirement: FindSwingHighs

The system SHALL identifies swing high points in price data using a fixed window approach for peak detection.

#### Scenario: MultipleSwingHighsFound
- **GIVEN** Price data with clear peaks
- **WHEN** findSwingHighs is called
- **THEN** Returns list of index-value pairs for each swing high

#### Scenario: NoSwingHighsInFlatData
- **GIVEN** Flat or consistently declining price data
- **WHEN** findSwingHighs is called
- **THEN** Returns empty list

### Requirement: FindSwingLows

The system SHALL identifies swing low points in price data using a fixed window approach for trough detection.

#### Scenario: MultipleSwingLowsFound
- **GIVEN** Price data with clear troughs
- **WHEN** findSwingLows is called
- **THEN** Returns list of index-value pairs for each swing low

#### Scenario: NoSwingLowsInRisingData
- **GIVEN** Consistently rising price data
- **WHEN** findSwingLows is called
- **THEN** Returns empty list

### Requirement: ComputeWideAndLoose

The system SHALL determines if the final contraction in a vcp pattern is considered wide-and-loose based on volatility threshold.

#### Scenario: FinalContractionIsWideAndLoose
- **GIVEN** Final contraction volatility exceeds threshold
- **WHEN** computeWideAndLoose is called
- **THEN** Returns true indicating wide-and-loose pattern

#### Scenario: FinalContractionIsTight
- **GIVEN** Final contraction volatility below threshold
- **WHEN** computeWideAndLoose is called
- **THEN** Returns false indicating tight contraction


## Sub-components

> `VCPPatternCalculator` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: AtrCalculation

> Implements: `_calculate_atr`

Calculates Average True Range (ATR) to measure price volatility over a specified period.

#### Requirement: CalculateATR

The system SHALL compute atr using high, low, and close price data over the specified period

#### Scenario: StandardAtrCalculation
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** price data with high, low, close values for 14 periods
- **WHEN** ATR is calculated with period 14
- **THEN** returns valid ATR value representing average volatility

### Sub-component: ZigzagSwingPoints

> Implements: `_zigzag_swing_points`

Identifies significant swing points in price data using zigzag analysis to filter out minor fluctuations.

#### Requirement: IdentifySwingPoints

The system SHALL detect significant price reversals by filtering out movements below threshold percentage

#### Scenario: ZigzagSwingDetection
- **GIVEN** price data with 5% threshold
- **WHEN** zigzag analysis is performed
- **THEN** returns only significant swing highs and lows exceeding threshold

### Sub-component: SwingHighDetection

> Implements: `_find_swing_highs`

Identifies swing high points in price data using a fixed window approach.

#### Requirement: FindSwingHighs

The system SHALL identify local maximum points where price is highest within the specified window

#### Scenario: SwingHighIdentification
- **GIVEN** array of high prices and window size of 5
- **WHEN** swing high detection is performed
- **THEN** returns list of (index, value) tuples for local maxima

### Sub-component: SwingLowDetection

> Implements: `_find_swing_lows`

Identifies swing low points in price data using a fixed window approach.

#### Requirement: FindSwingLows

The system SHALL identify local minimum points where price is lowest within the specified window

#### Scenario: SwingLowIdentification
- **GIVEN** array of low prices and window size of 5
- **WHEN** swing low detection is performed
- **THEN** returns list of (index, value) tuples for local minima

### Sub-component: ContractionBuilding

> Implements: `_build_contractions_from`

Constructs contraction patterns from swing points by analyzing successive high-low relationships.

#### Requirement: BuildContractions

The system SHALL create contraction objects from swing points showing decreasing volatility ranges

#### Scenario: ContractionPatternBuilding
- **GIVEN** series of swing highs and lows
- **WHEN** contraction building is performed
- **THEN** returns structured contraction patterns with decreasing ranges

### Sub-component: VcpValidation

> Implements: `_validate_vcp`

Validates whether identified contractions meet VCP pattern criteria and requirements.

#### Requirement: ValidateVCPPattern

The system SHALL verify contractions meet vcp rules including minimum count, decreasing volatility, and proper structure

#### Scenario: ValidVcpPattern
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** contractions with decreasing ranges and proper structure
- **WHEN** VCP validation is performed
- **THEN** returns true for valid VCP pattern

### Sub-component: VcpScoring

> Implements: `_score_vcp`

Calculates a numerical score for VCP pattern quality based on multiple criteria.

#### Requirement: ScoreVCPQuality

The system SHALL compute quality score based on contraction tightness, duration, and pattern characteristics

#### Scenario: VcpQualityScoring
- **GIVEN** validated VCP pattern with tight contractions
- **WHEN** scoring is performed
- **THEN** returns numerical score reflecting pattern quality

### Sub-component: PivotPriceRetrieval

> Implements: `_get_pivot_price`

Retrieves the pivot price level that serves as the breakout point for the VCP pattern.

#### Requirement: GetPivotPrice

The system SHALL identify the key price level that represents the breakout point from the contraction pattern

#### Scenario: PivotPriceIdentification
- **GIVEN** completed VCP pattern with final contraction
- **WHEN** pivot price is retrieved
- **THEN** returns the breakout price level

### Sub-component: WideLooseComputation

> Implements: `_compute_wide_and_loose`

Determines if the final contraction in the pattern is wide-and-loose based on threshold criteria.

#### Requirement: ComputeWideAndLoose

The system SHALL evaluate if final contraction exceeds width threshold indicating wide-and-loose characteristic

#### Scenario: WideLooseDetection
- **GIVEN** contractions with final contraction width and threshold of 0.15
- **WHEN** wide-and-loose computation is performed
- **THEN** returns true if final contraction exceeds threshold

### Sub-component: DictionaryAccess

> Implements: `d.get`

Retrieves values from dictionary objects with optional default values.

#### Requirement: GetDictionaryValue

The system SHALL retrieve value from dictionary using key with optional default if key not found

#### Scenario: DictionaryValueRetrieval
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** dictionary with key-value pairs and requested key
- **WHEN** dictionary get is performed
- **THEN** returns associated value or default if key missing

### Sub-component: ValueAccess

> Implements: `v.get`

Retrieves values from value objects or collections with optional default values.

#### Requirement: GetValueObject

The system SHALL retrieve value from value object using accessor with optional default if not found

#### Scenario: ValueObjectRetrieval
<!-- spec-gen-test: priority=high tags=regression (auto) -->
- **GIVEN** value object with accessor key
- **WHEN** value get is performed
- **THEN** returns associated value or default if accessor missing

> `skills/vcp-screener/scripts/calculators/relative_strength_calculator.py`

### Requirement: CalculateRelativeStrength

> Implementation: `calculate_relative_strength` in `skills\vcp-screener\scripts\calculators\relative_strength_calculator.py` · confidence: llm

The system SHALL computes weighted relative strength score for a single stock against s&p 500 using minervini's methodology with 40% weight on 3-month performance and decreasing weights for longer periods.

#### Scenario: ValidCalculationWithSufficientData
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Stock and S&P 500 price data with 252+ trading days
- **WHEN** calculateRelativeStrength is called
- **THEN** Returns weighted RS score between 0-100 with period breakdown

#### Scenario: InsufficientStockData
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Stock price data with fewer than 63 trading days
- **WHEN** calculateRelativeStrength is called
- **THEN** Returns score of 0 with error message about insufficient data

#### Scenario: PartialDataAvailable
- **GIVEN** Some periods have sufficient data while others have partial data
- **WHEN** calculateRelativeStrength is called
- **THEN** Uses available data with reduced weights and includes partial data notes

### Requirement: RankRelativeStrengthUniverse

> Implementation: `rank_relative_strength_universe` in `skills\vcp-screener\scripts\calculators\relative_strength_calculator.py` · confidence: llm

The system SHALL ranks multiple stocks by their weighted relative strength and assigns percentile-based scores, with population size adjustments to prevent inflated rankings in small universes.

#### Scenario: LargePopulationRanking
- **GIVEN** 20 or more stocks with valid weighted RS calculations
- **WHEN** rankRelativeStrengthUniverse is called
- **THEN** Assigns full percentile rankings with scores up to 100

#### Scenario: SmallPopulationCapping
- **GIVEN** Fewer than 20 stocks with valid weighted RS calculations
- **WHEN** rankRelativeStrengthUniverse is called
- **THEN** Caps maximum scores and percentiles to prevent inflated rankings

#### Scenario: InvalidStocksHandling
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Some stocks have weighted_rs=None due to insufficient data
- **WHEN** rankRelativeStrengthUniverse is called
- **THEN** Assigns score=0 and rs_percentile=0 to invalid stocks

## Technical Notes

- **Dependencies**: subprocess, tempfile, shutil, json
