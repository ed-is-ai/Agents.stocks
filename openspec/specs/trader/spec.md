# Trader Specification

> Generated by spec-gen v1.0.0 on 2026-05-10 22:19
> Source files: agents/trader/ingestion/base.py

## Purpose

Records buy/sell trades to SQLite database and computes portfolio profit and loss calculations.
*(analyzed in 2 chunks)*

## Entities

### IngestResult

> `agents/trader/ingestion/base.py`

Represents the outcome of a portfolio transaction ingestion operation, tracking success metrics and
errors.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Name of the file that was processed |
| portfolio | string | Portfolio identifier associated with the ingestion |
| trades_imported | integer | Number of trade transactions successfully imported |
| value_rows_imported | integer | Number of portfolio value rows successfully imported |
| errors | array[string] | List of error messages encountered during ingestion |

### IngestorBase

> `agents/trader/ingestion/base.py`

Abstract base class for processing portfolio transaction CSV files from staging directories.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| portfolio | string | Portfolio identifier that determines staging and processed directory paths |

**Relationships:**

- creates IngestResult (Produces IngestResult objects when processing files)
- uses TraderAgent (Collaborates with TraderAgent during file ingestion)

## Requirements

### Requirement: IngestResultValidation

The system SHALL validate IngestResult according to these rules:
- IngestResult is considered successful (ok) when errors list is empty

#### Scenario: SuccessfulIngestion
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** A CSV file with valid portfolio data
- **WHEN** Ingestion completes without errors
- **THEN** IngestResult.ok returns true and trades_imported > 0

#### Scenario: FailedIngestion
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** A CSV file with invalid data
- **WHEN** Ingestion encounters validation errors
- **THEN** IngestResult.ok returns false and errors list contains error messages

### Requirement: IngestorBaseValidation

The system SHALL validate IngestorBase according to these rules:
- Only CSV files are processed from staging directory
- Files named .gitkeep are excluded from processing
- Processed files are moved to avoid reprocessing
- Portfolio value CSV prevents duplicate entries based on date and portfolio key

#### Scenario: ProcessPendingFiles
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** CSV files exist in the staging directory
- **WHEN** run() method is called with a TraderAgent
- **THEN** Each file is processed and successful ones are moved to processed directory

#### Scenario: HandleFileCollision
- **GIVEN** A processed file with the same name already exists
- **WHEN** mark_processed() is called
- **THEN** File is renamed with timestamp suffix to avoid overwriting

#### Scenario: AppendPortfolioValues
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** New portfolio value rows to add
- **WHEN** _append_value_rows() is called
- **THEN** Only non-duplicate rows are appended to portfolio_value.csv

> `agents/trader/trader_agent.py`

### Requirement: RecordTrade

> Implementation: `Trade` in `models.py` · confidence: heuristic

The system SHALL validates and persists a new trade record with ticker, action, shares, price, and optional metadata.

#### Scenario: ValidBuyTrade
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid trade parameters with BUY action
- **WHEN** recordTrade is called
- **THEN** Trade is persisted to database with generated ID

#### Scenario: ValidSellTrade
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid trade parameters with SELL action
- **WHEN** recordTrade is called
- **THEN** Trade is persisted to database with generated ID

#### Scenario: InvalidAction
<!-- spec-gen-test: tags=smoke,regression (auto) -->
- **GIVEN** Trade with action not in BUY/SELL
- **WHEN** recordTrade is called
- **THEN** Validation error is raised

### Requirement: CalculatePortfolioPnL

The system SHALL computes profit and loss for the entire portfolio by analyzing all recorded trades.

#### Scenario: PortfolioWithRealizedGains
- **GIVEN** Multiple buy/sell pairs with profitable exits
- **WHEN** calculatePortfolioPnL is called
- **THEN** Returns positive total P&L and current positions

#### Scenario: PortfolioWithUnrealizedPositions
- **GIVEN** Open positions without corresponding sell trades
- **WHEN** calculatePortfolioPnL is called
- **THEN** Returns current positions with unrealized P&L

### Requirement: GetTradeHistory

> Implementation: `Trade` in `models.py` · confidence: heuristic

The system SHALL retrieves all recorded trades from the database ordered by date.

#### Scenario: AllTrades
- **GIVEN** Multiple trades exist in database
- **WHEN** getTradeHistory is called without ticker filter
- **THEN** Returns all trades ordered by date

#### Scenario: FilteredByTicker
- **GIVEN** Trades for multiple tickers exist
- **WHEN** getTradeHistory is called with specific ticker
- **THEN** Returns only trades for that ticker

### Requirement: InitializeDatabase

The system SHALL creates the trades table schema if it doesn't exist.

#### Scenario: FreshDatabase
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Database file doesn't exist
- **WHEN** initializeDatabase is called
- **THEN** Creates database file and trades table with proper schema

#### Scenario: ExistingDatabase
- **GIVEN** Database already exists with trades table
- **WHEN** initializeDatabase is called
- **THEN** No changes made to existing schema

> `orchestrator.py`

### Requirement: ExecuteFullPipeline

> Implementation: `pipeline` in `orchestrator.py` · confidence: llm

The system SHALL executes the complete trading pipeline including data extraction, stock scanning, analysis, alerting, and trading operations.

#### Scenario: NormalMarketHoursExecution
- **GIVEN** Market is open and no force flag
- **WHEN** pipeline is executed
- **THEN** All agents run in sequence and results are saved

#### Scenario: ForcedExecutionOutsideMarketHours
- **GIVEN** Market is closed but force flag is true
- **WHEN** pipeline is executed with force=True
- **THEN** Pipeline runs regardless of market hours

#### Scenario: PipelineWithDataExtraction
- **GIVEN** Extract flag is enabled
- **WHEN** pipeline is executed with extract=True
- **THEN** Data extraction agent runs before other agents

### Requirement: CheckMarketHours

> Implementation: `is_market_hours` in `orchestrator.py` · confidence: llm

The system SHALL determines if the current time falls within market trading hours (9:30 am - 4:00 pm est, weekdays only).

#### Scenario: DuringMarketHoursOnWeekday
- **GIVEN** Current time is between 9:30 AM and 4:00 PM on a weekday
- **WHEN** checkMarketHours is called
- **THEN** Returns true

#### Scenario: OutsideMarketHours
- **GIVEN** Current time is before 9:30 AM, after 4:00 PM, or on weekend
- **WHEN** checkMarketHours is called
- **THEN** Returns false

### Requirement: StartScheduledExecution

> Implementation: `main` in `run_tests.py` · confidence: llm

The system SHALL starts the scheduled execution of the trading pipeline using cron-based scheduling.

#### Scenario: StartSchedulerWithDefaultSettings
- **GIVEN** No command line arguments provided
- **WHEN** startScheduledExecution is called
- **THEN** Scheduler starts with default cron schedule

#### Scenario: StartWithForceExecution
- **GIVEN** Force flag is provided via command line
- **WHEN** startScheduledExecution is called with --force
- **THEN** Pipeline executes immediately regardless of market hours

## Technical Notes

- **Implementation**: `agents/trader/ingestion/base.py`
- **Dependencies**: Trade, Position, ExtractionAgent, ScannerAgent, AnalystAgent, AlertAgent, TraderAgent, AgentApp
