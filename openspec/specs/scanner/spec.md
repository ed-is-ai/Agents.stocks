# Scanner Specification

> Generated by spec-gen v1.0.0 on 2026-05-10 22:19
> Source files: models.py, agents/scanner/alpha_vantage_client.py, agents/scanner/congress_client.py

## Purpose

Orchestrates stock screening across multiple data sources (watchlists, VCP screener, TradingView)
and generates enriched stock records with technical indicators and institutional data.

## Entities

### StockScan

> `models.py`

Represents a comprehensive technical and fundamental analysis snapshot of a stock at a specific
point in time.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| ticker | string | Stock symbol identifier |
| as_of | string | Date of the scan |
| price | float | Current stock price |
| price_history | list[float] | Weekly closes for past 52 weeks |
| sma10 | float | 10-day simple moving average |
| sma30 | float | 30-day simple moving average |
| sma50 | float | 50-day simple moving average |
| sma150 | float | 150-day simple moving average |
| sma200 | float | 200-day simple moving average |
| rsi14 | float | 14-day Relative Strength Index |
| volume | int | Current trading volume |
| rel_volume | float | Relative volume compared to average |
| high_52w | float | 52-week high price |
| low_52w | float | 52-week low price |
| sector | string | GICS sector classification |
| eps_growth | float | Current quarter EPS growth YoY |
| annual_eps_growth | float | 3-year annual EPS CAGR |
| roe | float | Return on equity |
| pe_ratio | float | Trailing twelve-month P/E ratio |
| source | string | Screener source identifier |

**Relationships:**

- has-one StockAnalysis (Stock scan can have associated analysis)

### MomentumScore

> `models.py`

Technical momentum scoring system based on CANSLIM methodology using price/volume proxies, with each
component scored 0-2.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| C | int | Weekly price change momentum proxy |
| A | int | Position within 52w range proxy |
| N | int | Proximity to 52w high proxy |
| S | int | Relative volume supply & demand proxy |
| L | int | RSI leader vs laggard proxy |
| I | int | SMA structure institutional proxy |
| M | int | SMA alignment market direction proxy |

**Relationships:**

- belongs to StockAnalysis (Momentum score is part of stock analysis)

### StockAnalysis

> `models.py`

Comprehensive technical analysis of a stock including scoring, entry/exit points, and risk
management parameters.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| score | int | Overall analysis score 1-10 |
| stage | string | Current market stage classification |
| entry_zone | string | Entry timing classification |
| entry_price | float | Next entry price target |
| stop_loss | float | Stop loss price level |
| risk_pct | float | Risk percentage from entry to stop |
| volume_confirmed | bool | Whether breakout has volume confirmation |
| fresh_breakout | bool | Whether this is a new breakout |
| summary | string | Analysis summary text |

**Relationships:**

- has-one CANSLIMScore (Analysis includes CANSLIM scoring)
- has-one MomentumScore (Analysis includes momentum scoring)
- belongs to StockRecord (Analysis belongs to a stock record)

### Trade

> `models.py`

A recorded buy or sell transaction for portfolio tracking.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | int | Unique trade identifier |
| ticker | string | Stock symbol |
| action | string | BUY or SELL action |
| shares | float | Number of shares traded |
| price | float | Execution price per share |
| date | string | Trade execution date |
| notes | string | Optional trade notes |
| stop_loss | float | Initial stop loss level |
| entry_price | float | Analyst pivot entry price |

**Relationships:**

- contributes-to Position (Trades build up positions)

### Position

> `models.py`

An open portfolio position with live P&L tracking and Minervini-style exit metrics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| ticker | string | Stock symbol |
| shares | float | Total shares held |
| avg_cost | float | Average cost per share |
| current_price | float | Current market price |
| total_cost | float | Total investment amount |
| current_value | float | Current market value |
| unrealised_pnl | float | Unrealized profit/loss amount |
| unrealised_pnl_pct | float | Unrealized profit/loss percentage |
| entry_price | float | Pivot entry price from first buy |
| stop_loss | float | Initial stop loss level |
| profit_target_20 | float | 20% profit target |
| profit_target_25 | float | 25% profit target |

**Relationships:**

- composed-of Trade (Position is built from multiple trades)
- has-one ExitSignal (Position may have exit signal)

### ExitSignal

> `models.py`

Exit or add signal recommendation for a held position.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| action | string | EXIT or ADD recommendation |
| reason | string | Rationale for the signal |

**Relationships:**

- belongs to Position (Exit signal applies to a specific position)

### AlphaVantageClient

> `agents/scanner/alpha_vantage_client.py`

Rate-limited API client for fetching fundamental financial data from Alpha Vantage service with
session-based caching.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| api_key | string | Alpha Vantage API key for authentication |
| _cache | dict | In-memory cache for API responses per session |
| _last_call | float | Timestamp of last API call for rate limiting |
| enabled | boolean | Whether client is enabled based on API key availability |

### CompanyOverview

> `agents/scanner/alpha_vantage_client.py`

Fundamental financial data for a company retrieved from Alpha Vantage OVERVIEW endpoint.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| Symbol | string | Stock ticker symbol |
| Sector | string | Business sector classification |
| PERatio | string | Price-to-earnings ratio |
| ReturnOnEquityTTM | string | Return on equity trailing twelve months |
| QuarterlyEarningsGrowthYOY | string | Quarterly earnings growth year-over-year |
| EPS | string | Earnings per share trailing twelve months |

**Relationships:**

- fetched-by AlphaVantageClient (Overview data is fetched by the Alpha Vantage client)

### EarningsData

> `agents/scanner/alpha_vantage_client.py`

Historical quarterly and annual earnings data for calculating precise growth metrics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| quarterlyEarnings | array | List of quarterly earnings reports with reportedEPS |
| annualEarnings | array | List of annual earnings reports with reportedEPS |

**Relationships:**

- fetched-by AlphaVantageClient (Earnings data is fetched by the Alpha Vantage client)

### CongressStats

> `agents/scanner/congress_client.py`

Represents congressional trading statistics for a single stock ticker over a 12-month lookback
period, tracking buy and sell transactions by Congress members.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| buys | int | Total number of purchase transactions by all Congress members |
| sells | int | Total number of sale transactions by all Congress members |
| senate_buys | int | Number of purchase transactions by Senate members only |
| senate_sells | int | Number of sale transactions by Senate members only |

### CongressClient

> `agents/scanner/congress_client.py`

Rate-limited web scraper that retrieves congressional trading data from QuiverQuant for stock
tickers, providing caching and polite request handling.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| _cache | dict[str, CongressStats | None] | Cache of ticker to stats mappings |
| _last_call | float | Timestamp of last API call for rate limiting |
| _session | requests.Session | HTTP session with configured headers |

**Relationships:**

- creates CongressStats (Client creates CongressStats instances from scraped data)

## Requirements

### Requirement: StockScanValidation

The system SHALL validate StockScan according to these rules:
- Price must be positive
- Volume must be non-negative integer
- 52-week high must be >= 52-week low
- Percentage values should be in decimal format (0.25 = 25%)

#### Scenario: CreateStockScan
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid ticker and market data
- **WHEN** StockScan is instantiated with required fields
- **THEN** Stock scan record is created with technical indicators calculated

### Requirement: MomentumScoreValidation

The system SHALL validate MomentumScore according to these rules:
- Each component score must be between 0 and 2 inclusive
- Total score cannot exceed 14

#### Scenario: CalculateMomentumScore
- **GIVEN** Stock with technical indicators
- **WHEN** MomentumScore is calculated
- **THEN** Each CANSLIM component receives score 0-2 based on technical criteria

### Requirement: StockAnalysisValidation

The system SHALL validate StockAnalysis according to these rules:
- Score must be between 1 and 10 inclusive
- Entry zone must be one of: broken_out, approaching, getting_close, extended, far
- Risk percentage should be positive when stop loss is set
- Entry price should be above current price for breakout strategies

#### Scenario: GenerateStockAnalysis
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** StockScan with complete technical data
- **WHEN** Analysis is performed
- **THEN** StockAnalysis is created with scoring, entry/exit points, and risk parameters

### Requirement: TradeValidation

The system SHALL validate Trade according to these rules:
- Action must be either BUY or SELL
- Shares must be positive
- Price must be positive
- Date must be valid date string

#### Scenario: RecordBuyTrade
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Decision to purchase stock
- **WHEN** Trade is executed and recorded
- **THEN** Trade record is created and position is updated or created

#### Scenario: RecordSellTrade
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Existing position to reduce or close
- **WHEN** Sell trade is executed
- **THEN** Trade record is created and position is reduced or closed

### Requirement: PositionValidation

The system SHALL validate Position according to these rules:
- Shares must be positive for open position
- Average cost must be positive
- Total cost should equal shares * avg_cost
- Profit targets should be above entry price

#### Scenario: OpenNewPosition
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** First buy trade for a ticker
- **WHEN** Trade is processed
- **THEN** New position is created with initial metrics

#### Scenario: UpdatePositionP&L
- **GIVEN** Open position with current market price
- **WHEN** Position is refreshed
- **THEN** Unrealized P&L and percentages are calculated

### Requirement: ExitSignalValidation

The system SHALL validate ExitSignal according to these rules:
- Action must be either EXIT or ADD
- Reason must be provided for all signals

#### Scenario: GenerateExitSignal
- **GIVEN** Position with deteriorating technical conditions
- **WHEN** Exit evaluator runs
- **THEN** EXIT signal is generated with specific reason

### Requirement: AlphaVantageClientValidation

The system SHALL validate AlphaVantageClient according to these rules:
- API key must be provided via constructor or ALPHA_VANTAGE_API_KEY environment variable
- Minimum 12 seconds between API calls (5 calls per minute limit)
- Maximum 25 API calls per day on free tier
- Request timeout of 15 seconds

#### Scenario: FetchCompanyOverview
- **GIVEN** Valid API key and stock symbol
- **WHEN** get_overview() is called
- **THEN** Company overview data is returned and cached for session

#### Scenario: RateLimitingEnforcement
- **GIVEN** Previous API call made less than 12 seconds ago
- **WHEN** Another API call is made
- **THEN** Client sleeps until minimum interval is satisfied

#### Scenario: CacheHitScenario
- **GIVEN** Data previously fetched and cached for a symbol
- **WHEN** Same data is requested again
- **THEN** Cached data is returned without making API call

#### Scenario: CalculateQuarterlyEPSGrowth
- **GIVEN** At least 5 quarters of earnings data available
- **WHEN** get_quarterly_eps_growth() is called
- **THEN** Year-over-year EPS growth is calculated from most recent vs year-ago quarter

### Requirement: CompanyOverviewValidation

The system SHALL validate CompanyOverview according to these rules:
- Symbol field must be present for valid overview data
- Percentage fields are parsed as decimal values
- Fields may contain 'None', '-', or 'N/A' for missing data

#### Scenario: ParseFundamentalMetrics
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Valid company overview data from API
- **WHEN** get_fundamentals() processes the data
- **THEN** Percentage strings are converted to decimal values and missing data is handled as None

### Requirement: EarningsDataValidation

The system SHALL validate EarningsData according to these rules:
- At least 5 quarterly data points required for quarterly EPS growth calculation
- At least 3 annual data points required for CAGR calculation
- Year-ago quarter EPS must be positive for valid growth calculation
- Base year EPS must be positive for CAGR calculation

#### Scenario: CalculateQuarterlyEPSGrowth
- **GIVEN** 5 or more quarterly earnings data points with positive year-ago EPS
- **WHEN** get_quarterly_eps_growth() is called
- **THEN** Year-over-year growth is calculated as (recent - year_ago) / year_ago

#### Scenario: CalculateAnnualEPSCAGR
- **GIVEN** 3 or more annual earnings data points with positive EPS values
- **WHEN** get_annual_eps_cagr() is called
- **THEN** 3-year compound annual growth rate is calculated using up to 4 data points

### Requirement: CongressStatsValidation

The system SHALL validate CongressStats according to these rules:
- All counts must be non-negative integers
- Senate buys must be less than or equal to total buys
- Senate sells must be less than or equal to total sells

#### Scenario: CalculateTotalCongressionalActivity
- **GIVEN** CongressStats with buys=5, sells=3, senate_buys=2, senate_sells=1
- **WHEN** accessing total activity metrics
- **THEN** Total buys is 5 and total sells is 3

#### Scenario: TrackSenateSpecificActivity
- **GIVEN** CongressStats with senate_buys=2, senate_sells=1
- **WHEN** analyzing Senate trading patterns
- **THEN** Senate has 2 buy transactions and 1 sell transaction

### Requirement: CongressClientValidation

The system SHALL validate CongressClient according to these rules:
- Minimum 3 seconds between API calls
- Requests must include proper User-Agent headers
- Only transactions within last 365 days are counted
- Must handle 404 responses gracefully

#### Scenario: FetchCongressionalStatsForTicker
- **GIVEN** CongressClient instance and valid ticker symbol
- **WHEN** get_stats() is called
- **THEN** Returns CongressStats with parsed buy/sell counts from last 12 months

#### Scenario: HandleRateLimiting
- **GIVEN** CongressClient with recent API call
- **WHEN** Another request is made within 3 seconds
- **THEN** Client waits before making the request

#### Scenario: CacheResults
- **GIVEN** CongressClient that has fetched stats for a ticker
- **WHEN** Same ticker is requested again
- **THEN** Returns cached result without making new HTTP request

#### Scenario: HandleMissingData
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** CongressClient and ticker with no congressional trading data
- **WHEN** get_stats() is called
- **THEN** Returns None and caches the null result

> `agents/scanner/scanner_agent.py`

### Requirement: RunStockScan

> Implementation: `StockScan` in `models.py` · confidence: heuristic

The system SHALL executes comprehensive stock scanning workflow across all configured sources and returns enriched stock records.

#### Scenario: FullScanExecution
- **GIVEN** Multiple data sources are available
- **WHEN** run is called with no payload
- **THEN** Returns consolidated stock records from all sources with technical and institutional data

#### Scenario: TargetedTickerScan
- **GIVEN** Specific tickers provided in payload
- **WHEN** run is called with ticker list
- **THEN** Returns stock records only for specified tickers

### Requirement: FetchStockData

The system SHALL retrieves historical price and volume data for a given ticker from yahoo finance.

#### Scenario: ValidTickerDataRetrieval
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** A valid stock ticker
- **WHEN** fetch_stock_data is called
- **THEN** Returns DataFrame with OHLCV data for the past year

#### Scenario: InvalidTickerHandling
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** An invalid or delisted ticker
- **WHEN** fetch_stock_data is called
- **THEN** Returns None without raising exceptions

### Requirement: ComputeTechnicalIndicators

The system SHALL calculates technical analysis indicators including moving averages, rsi, and volatility metrics from price data.

#### Scenario: CompleteTechnicalAnalysis
- **GIVEN** OHLCV DataFrame with sufficient data points
- **WHEN** compute_technicals is called
- **THEN** Returns dictionary with SMA, RSI, ATR, and other technical indicators

#### Scenario: InsufficientDataHandling
- **GIVEN** DataFrame with fewer than required periods
- **WHEN** compute_technicals is called
- **THEN** Returns partial indicators or NaN values for incomplete calculations

> `agents/scanner/scanner_agent.py`

### Requirement: LoadWatchlist

> Implementation: `load_watchlist` in `agents\scanner\scanner_agent.py` · confidence: llm

The system SHALL loads unique stock tickers from extraction results, flattening all grouped sources into a single list.

#### Scenario: SuccessfulWatchlistLoading
<!-- spec-gen-test: tags=smoke,regression (auto) -->
- **GIVEN** extraction_results.json exists with ticker data
- **WHEN** load_watchlist is called
- **THEN** Returns deduplicated list of all tickers from extraction sources

#### Scenario: MissingExtractionFile
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** extraction_results.json does not exist
- **WHEN** load_watchlist is called
- **THEN** Raises FileNotFoundError or returns empty list

### Requirement: LoadWhaleWisdomContext

> Implementation: `load_ww_context` in `agents\scanner\scanner_agent.py` · confidence: llm

The system SHALL loads institutional investor context data including filer counts and rankings for each ticker.

#### Scenario: ContextDataAvailable
- **GIVEN** whale_wisdom_context.json exists
- **WHEN** load_ww_context is called
- **THEN** Returns per-ticker institutional activity metrics

### Requirement: LoadSourceMap

> Implementation: `load_source_map` in `agents\scanner\scanner_agent.py` · confidence: llm

The system SHALL creates mapping of tickers to their data source flags indicating presence in stocktwits and whalewisdom.

#### Scenario: SourceMappingGeneration
- **GIVEN** Extraction results contain source information
- **WHEN** load_source_map is called
- **THEN** Returns ticker-to-source-flags mapping

## Technical Notes

- **Implementation**: `models.py, agents/scanner/alpha_vantage_client.py, agents/scanner/congress_client.py`
- **Dependencies**: StockRecord, CongressClient, yfinance, pandas, json
