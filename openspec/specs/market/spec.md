# Market Specification

> Generated by spec-gen v1.0.0 on 2026-05-10 22:19

## Purpose

Provides rate-limited access to Financial Modeling Prep API endpoints for CANSLIM component analysis
with automatic retry and session caching. *(analyzed in 3 chunks)*

## Requirements

> `skills/canslim-screener/scripts/fmp_client.py`

### Requirement: GetQuote

The system SHALL retrieves current quote data for specified symbols with rate limiting and retry logic.

#### Scenario: SuccessfulQuoteRetrieval
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid symbols and API key
- **WHEN** getQuote is called
- **THEN** Returns current quote data for symbols

#### Scenario: RateLimitHandling
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** API rate limit exceeded
- **WHEN** getQuote is called
- **THEN** Waits 0.3s between requests and retries on 429 errors

### Requirement: GetHistoricalData

The system SHALL retrieves historical price data for symbols with configurable time series length.

#### Scenario: HistoricalDataRetrieval
- **GIVEN** Valid symbols and timeseries parameter
- **WHEN** getHistoricalData is called
- **THEN** Returns historical price data for specified period

#### Scenario: EndpointFallback
- **GIVEN** Primary endpoint fails
- **WHEN** getHistoricalData is called
- **THEN** Falls back from stable endpoint to v3 endpoint

### Requirement: BuildEndpointUrl

The system SHALL constructs appropriate api endpoint urls with fallback logic between stable and v3 versions.

#### Scenario: URLConstructionWithStableEndpoint
- **GIVEN** Endpoint type and symbols
- **WHEN** buildEndpointUrl is called
- **THEN** Returns properly formatted stable API URL with parameters

#### Scenario: URLConstructionWithV3Fallback
- **GIVEN** Stable endpoint format fails
- **WHEN** buildEndpointUrl is called
- **THEN** Returns v3 API URL format as fallback

> `skills/canslim-screener/scripts/finviz_stock_client.py`

### Requirement: GetInstitutionalOwnership

The system SHALL retrieves institutional ownership percentage and transaction data for a given stock symbol from finviz.

#### Scenario: ValidStockSymbolWithInstitutionalData
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** A valid stock symbol with available institutional data on Finviz
- **WHEN** getInstitutionalOwnership is called
- **THEN** Returns institutional ownership and transaction percentages as floats

#### Scenario: StockSymbolWithMissingInstitutionalData
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** A valid stock symbol but institutional data shows '-' on Finviz
- **WHEN** getInstitutionalOwnership is called
- **THEN** Returns null values for percentages with no error

#### Scenario: InvalidOrNonExistentStockSymbol
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** An invalid stock symbol that returns 404 or error from Finviz
- **WHEN** getInstitutionalOwnership is called
- **THEN** Returns null values with error message

#### Scenario: CachedInstitutionalDataRequest
- **GIVEN** Previously fetched institutional data exists in cache
- **WHEN** getInstitutionalOwnership is called for same symbol
- **THEN** Returns cached data without making HTTP request

### Requirement: GetStockData

The system SHALL retrieves complete stock data dictionary containing all available fields from finviz stock page.

#### Scenario: ValidStockSymbol
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** A valid stock symbol available on Finviz
- **WHEN** getStockData is called
- **THEN** Returns dictionary with all parsed Finviz data fields

#### Scenario: NetworkOrParsingFailure
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Network timeout or HTML parsing error occurs
- **WHEN** getStockData is called
- **THEN** Returns null and logs warning message

#### Scenario: CachedStockDataRequest
- **GIVEN** Previously fetched stock data exists in cache
- **WHEN** getStockData is called for same symbol
- **THEN** Returns cached data without making HTTP request

### Requirement: RateLimitedFetch

The system SHALL performs http request to finviz with rate limiting to avoid being blocked, includes retry logic and error handling.

#### Scenario: RateLimitEnforcement
- **GIVEN** Previous request was made less than rate_limit_seconds ago
- **WHEN** rateLimitedFetch is called
- **THEN** Sleeps until rate limit period has elapsed before making request

#### Scenario: SuccessfulHTTPResponse
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Finviz returns 200 status code with valid HTML
- **WHEN** rateLimitedFetch is called
- **THEN** Parses HTML and returns structured data dictionary

#### Scenario: HTTPErrorResponse
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Finviz returns non-200 status code
- **WHEN** rateLimitedFetch is called
- **THEN** Logs warning and returns null

> `skills/market-top-detector/scripts/calculators/breadth_calculator.py`

### Requirement: CalculateBreadthDivergence

> Implementation: `calculate_breadth_divergence` in `skills\market-top-detector\scripts\calculators\breadth_calculator.py` · confidence: llm

The system SHALL calculates market breadth divergence score based on percentage of stocks above moving averages and index position relative to highs.

#### Scenario: CriticalDivergenceWhenIndexNearHighs
- **GIVEN** Index within 5% of highs and 200DMA breadth < 40%
- **WHEN** calculateBreadthDivergence is called
- **THEN** Returns score of 100 with CRITICAL signal

#### Scenario: HealthyBreadthSupportingIndex
- **GIVEN** 200DMA breadth >= 70%
- **WHEN** calculateBreadthDivergence is called
- **THEN** Returns low score with HEALTHY signal

#### Scenario: MissingBreadthDataHandling
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** breadth_200dma is None
- **WHEN** calculateBreadthDivergence is called
- **THEN** Returns neutral score of 50 with NO DATA signal

#### Scenario: ScoreAdjustmentForIndexNotNearHighs
- **GIVEN** Index > 5% below highs
- **WHEN** calculateBreadthDivergence is called
- **THEN** Raw score is halved as divergence less meaningful

#### Scenario: 50DMASupplementaryAdjustment
- **GIVEN** 50DMA breadth < 30% and index near highs
- **WHEN** calculateBreadthDivergence is called
- **THEN** Adds 10 points to raw score as extra warning

### Requirement: Score200DMABreadth

> Implementation: `TestScore200dmaBreadth` in `skills\market-top-detector\scripts\tests\test_breadth.py` · confidence: heuristic

The system SHALL scores market breadth based on percentage of stocks above 200-day moving average using calibrated thresholds.

#### Scenario: CriticalBreadthWeakness
- **GIVEN** 200DMA breadth < 40%
- **WHEN** score200DMABreadth is called
- **THEN** Returns score of 100

#### Scenario: HealthyBreadth
- **GIVEN** 200DMA breadth >= 70%
- **WHEN** score200DMABreadth is called
- **THEN** Returns score of 5

#### Scenario: LinearInterpolationBetweenThresholds
- **GIVEN** 200DMA breadth between 40-50%
- **WHEN** score200DMABreadth is called
- **THEN** Returns linearly interpolated score between 100 and 55

> `skills/market-top-detector/scripts/calculators/distribution_day_calculator.py`

### Requirement: CalculateDistributionDays

> Implementation: `calculate_distribution_days` in `skills\market-top-detector\scripts\calculators\distribution_day_calculator.py` · confidence: llm

The system SHALL analyzes s&p 500 and nasdaq price/volume data to count distribution days and stalling days, returning a comprehensive score with clustering analysis.

#### Scenario: CriticalDistributionDetected
- **GIVEN** 6 or more effective distribution days in recent 25 trading days
- **WHEN** calculateDistributionDays is called
- **THEN** Returns score of 100 with CRITICAL signal

#### Scenario: O'NeilWarningThresholdReached
- **GIVEN** 4-5 effective distribution days detected
- **WHEN** calculateDistributionDays is called
- **THEN** Returns score of 75-90 with WARNING signal

#### Scenario: ClusteringPenaltyApplied
- **GIVEN** Distribution days are concentrated in recent 5 days with clustering factor > 0.5
- **WHEN** calculateDistributionDays is called
- **THEN** Applies 15% score increase penalty up to maximum of 100

#### Scenario: HigherIndexCountUsed
- **GIVEN** S&P 500 has 3 distribution days and NASDAQ has 5 distribution days
- **WHEN** calculateDistributionDays is called
- **THEN** Uses NASDAQ as primary index with higher effective count

### Requirement: CountDistributionDays

> Implementation: `TestCountDistributionDays` in `skills\market-top-detector\scripts\tests\test_distribution_day.py` · confidence: heuristic

The system SHALL counts distribution days (price drops >= 0.2% on higher volume) and stalling days (volume up but price gain < 0.1%) within 25 trading day window.

#### Scenario: DistributionDayIdentified
- **GIVEN** Price drops >= 0.2% and volume increases from previous day
- **WHEN** countDistributionDays analyzes daily data
- **THEN** Increments distribution day count and adds detail record

#### Scenario: StallingDayIdentified
- **GIVEN** Volume increases but price gain is less than 0.1%
- **WHEN** countDistributionDays analyzes daily data
- **THEN** Increments stalling day count and adds detail record

### Requirement: CalculateClusteringFactor

The system SHALL analyzes temporal clustering of distribution events by calculating ratio of recent events (last 5 days) to total events.

#### Scenario: HighClusteringDetected
- **GIVEN** Most distribution days occurred in recent 5 trading days
- **WHEN** calculateClusteringFactor is called
- **THEN** Returns clustering factor > 0.5 indicating concentrated selling

### Requirement: ScoreDistributionDays

> Implementation: `TestScoreDistributionDays` in `skills\market-top-detector\scripts\tests\test_distribution_day.py` · confidence: heuristic

The system SHALL converts effective distribution day count to 0-100 score using o'neil's thresholds.

#### Scenario: CriticalThresholdScoring
- **GIVEN** Effective count >= 6 distribution days
- **WHEN** scoreDistributionDays is called
- **THEN** Returns maximum score of 100

#### Scenario: WarningThresholdScoring
- **GIVEN** Effective count is 4 distribution days
- **WHEN** scoreDistributionDays is called
- **THEN** Returns score of 75

## Technical Notes

- **Dependencies**: requests, BeautifulSoup
